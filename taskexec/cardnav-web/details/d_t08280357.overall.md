# ai.lovemoney.live 全量任务说明与接手基线

## 已确认目标

将本仓库 `D:\work\dock\cardnav-web` 从原测试项目改为 `ai.lovemoney.live` 网站，并发布到 `206.119.177.74`。发布参考 `D:\phpStudy\WWW\likeshop\howtorunvpsnew.md`；该文档可提供流程和登录方式，但任何密码、密钥、连接串都不能写入仓库、任务清单、日志或 QA 报告。

本次讨论、README、设计文档和本 tasklist 是后续开发及其他 AI 接手的共同基线。README 及设计文档必须更新；除用户没有指定的第三方许可证外，仓库中可见的旧品牌文案也要随页面范围一起处理。

## 公开网站与内容范围

- 所有实际网站页面都在范围内：中文、英文、俄文首页、向导、About、隐私、免责声明、合作页、动态页面及其 SEO 标题、描述、canonical、结构化数据和数据库可见内容。
- 所有面向用户的 `cardnav.xyz` 改为 `ai.lovemoney.live`；删除“卡网大全”四个字和相同旧品牌的可见表达。源码内部遗留文件名可在不影响用户可见内容时保留，但不得产生旧域名的网页输出、请求或 SEO URL。
- 首页截图中标出的整块 Hero/clear area 必须删除，不留下空白占位。
- 顶部/移动端社群入口：移除 GitHub；保留 Telegram、X 和新增 QQ 图标，但均为明确的空链接/无跳转占位，以后可替换。任何原 Telegram 群链接必须移除，网站中不得再跳转到 Telegram。
- 公告改为纯文字，不得使用 Telegram 或其他外跳链接。
- 赞助商列表仅保留图片；图片不可点击，不显示名称、介绍、套餐、价格、按钮或外链，以便日后直接替换图片。

## 数据库迁移范围

- 从此弃用 PostgreSQL。运行时依赖、环境变量、README、部署和数据库代码都必须采用 MySQL。
- 开发机使用本机 MySQL 数据库 `ailovemoney`；账户信息只在本机运行时读取，不可提交。
- 首先只读检查原 PostgreSQL：若有数据，必须迁移到本地 MySQL，并对重要表做数量核对；若为空，记录空迁移，不制造伪数据。
- 开发阶段必须能从本地导出 MySQL SQL，复制到服务器后导入服务器本地 MySQL。导出文件/运行命令不得含秘密；服务器导入在发布任务中执行。
- 需要支持的公开运行表包含快照、商店/商品/搜索词、网关/模型价格、官方价格、排行榜；空库下所有公开路由读取与提交路径也必须正常返回，而不是使用 PostgreSQL 专用 SQL 后运行时失败。

## 服务器与端口边界

- 目标服务器：`206.119.177.74`。网站公开入口使用 HTTP `80` 端口作为网站根路径，目标域名为 `ai.lovemoney.live`。
- LikeShop 已有服务使用 `http://dtch.yg2022.top:8086/shop/`、`http://dtch.yg2022.top:8090/mobile/`、`http://dtch.yg2022.top:8095/admin/`，必须保持不变。
- 先诊断服务器 80 端口、Apache 虚拟主机、Node、MySQL、磁盘、DNS 和现存服务。若 80 端口有与 LikeShop 无关的占用，先备份可回滚配置再清理；不清理或变更 LikeShop 的三个端口配置。
- 将本项目部署到独立目录、使用独立 Node 内部监听端口，由 Apache 反向代理到 80；服务需受控、自启，并使用服务器本地 MySQL。
- 若 DNS 未生效，可先以正确 Host 头验证；最终仍须在真实域名入口复验。发布完成后检查本项目、数据库连接和 LikeShop 三个既有入口。

## 执行与验收约束

- 每个任务要独立锁定、提交、记录真实 commit hash、写 QA 报告和自审结果，便于其他 AI 从 tasklist、detail、tasklog 和 QA 文件继续。
- 任何浏览器可见任务未完成实际浏览器验证，不得标记 `done`；可标 `partial` 并继续后续无依赖任务。
- 不提交密码、私钥、数据库 dump、`.env` 或服务器登录资料。保留用户既有未关联改动，禁止用破坏性 Git 重置。
