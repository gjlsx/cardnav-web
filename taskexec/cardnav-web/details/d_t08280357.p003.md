# p003 本地数据迁移和导出

只读检查原 PostgreSQL 数据。若有记录，迁移到本地 MySQL `ailovemoney` 并比较记录数；若为空，明确记录空迁移事实。提供不含凭据的、可由开发者执行的 SQL 导出和服务器导入脚本/文档。服务器实际导入在 p005 的受控部署步骤执行。

验收：本机 MySQL 表和记录数证据，导出 SQL 可以解析/导入临时库，README 说明本机到服务器的流程。
