# p005 VPS 独立发布

以 `D:\phpStudy\WWW\likeshop\howtorunvpsnew.md` 为参考读取登录方式，但不得把任何秘密写入仓库。先诊断 `206.119.177.74` 的 Apache、Node、MySQL、80 端口、磁盘和 LikeShop 监听端口。只在确认 80 端口占用物与 LikeShop 无关后备份并清理它。部署到独立目录，初始化服务器本地 MySQL，导入 p003 SQL，使用内部 Node 端口并由 Apache 80 反向代理。不得改动 LikeShop 8086/8090/8095 配置。

验收：服务自启、Apache 配置检查通过、主机头/域名 HTTP 响应正确、数据库连接成功。
