# p006 线上回归与交接

从真实入口验证 `http://ai.lovemoney.live/`（或 DNS 未更新时以主机头验证），复查三语公开页、响应标识和服务器 MySQL。分别确认 LikeShop 的 `8086/shop/`、`8090/mobile/`、`8095/admin/` 仍有响应。补齐 README 和实施计划的最终状态、回滚位置和接手说明；不得记录秘密。

验收：QA 报告包含线上 URL/状态、端口隔离结果、回滚位置和未决风险。
