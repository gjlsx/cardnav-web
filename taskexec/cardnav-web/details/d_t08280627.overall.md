# 审查修复完整要求

审查对象是已归档的 `tasklist08280357.md` 全量实现及其线上发布。原始业务目标保持不变：ai.lovemoney.live、80/443 独立站、MySQL 替代 PostgreSQL、迁移公开快照、LikeShop 仅保留 8086/8090/8095。

审查已确认的缺陷：`src/store.ts` 的公开函数在开头返回 MySQL helper，却保留其后的 PostgreSQL SQL、`@ts-nocheck` 和旧类型分支。这使未来维护可意外恢复 PostgreSQL 依赖；同时新 helper 绕开了 `public_snapshot_entries`，9 条迁移的公开数据不保证被页面读取。

修复必须：删除不可达 PostgreSQL 分支；保留所有既有导出 API；使用 mysql2 和 MySQL 5.7 兼容 SQL；严格类型检查零错误；MySQL 快照为可用时优先读取，空快照时走实际表；不记录秘密；最后验证线上数据而非仅 HTTP 200。
