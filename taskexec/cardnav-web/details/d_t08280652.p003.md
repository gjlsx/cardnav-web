# p003：网页展示、验证与独立站发布

对现有 `/shops`、首页精选、`/llm-gateway` 等实际读取样例的入口做最小 UI 调整：让分类、价格、库存、商家、更新时间和来源/样例性质易读，样例不会混同为实时排名。采用本站组件与 CSS，不克隆参考站整页，不把第三方截图/商标作为站点资产，不新增购买导流。

需增加/调整的可见文案在中、英、俄页面中一致说明“参考样例/非实时，前往原平台核验”；不能恢复已移除的旧品牌、社群链接或赞助商外链。

验证与发布：

1. 本机运行 MySQL 样例脚本、`pnpm test`、`pnpm run typecheck`、`pnpm run build`；浏览器从页面入口验证首页精选、商品页、网关页样例和免责声明。
2. 按既有独立站部署流程发布到 `206.119.177.74` 的 ai.lovemoney 站；部署包不含 `.env`、SQL 导出或秘密。线上 MySQL 使用服务器本地 MySQL，生产导入只包含本次幂等样例。
3. 验证 `ai.lovemoney.live` HTTPS 和 HTTP:80，以及 LikeShop 三个既有地址仍可访问；记录状态码和页面可见证据，不记录凭证。

验收：生产页面显示的是同一批可追溯样例；没有误导性实时声明；独立站和 LikeShop 端口都通过检查。
