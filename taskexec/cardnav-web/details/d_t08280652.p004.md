# p004：本地自动采集的头脑风暴（不实施）

基于 p001/p002 的实际映射，写入 `docs/local-collection-brainstorm.md`。这是以后待用户确认的计划，不能创建采集器、调度器、Windows 任务计划、cron、远程服务或实际批量请求。

方案至少需覆盖：

1. 来源登记、授权/robots/条款人工检查、公开数据白名单和不采集项目；区分 API 中转站、商业小店、公开聚合比价页。
2. 本机运行边界：单一 CLI/脚本入口、显式 source allowlist、dry-run、人工审批导入、dotenv 秘密隔离、日志脱敏；不得让浏览器或生产站主动抓取。
3. 采集适配层（HTML/API 仅在明确许可时）、速率/并发/退避、时间戳、证据快照摘要、图片许可策略、结构变更检测。
4. 标准化、币种/计费单位、库存状态、品牌/商品别名、来源 URL、数据质量、重复项/异常价格、人工复核和可回滚的 staging-to-publish 流程。
5. MySQL 模型演进和快照发布：source/provenance、run/audit、实体表、只读公开快照、事务/版本切换、旧数据保留与过期策略。
6. Windows Task Scheduler 本地定时建议、失败告警、指标、运行手册、成本/风险；明确每项所需的用户确认。

交付还需给出分阶段里程碑及“批准后第一个小任务”的 TaskExec 建议，但不能将该建议当成本次授权。
