# p007 文档与 README 更新

## 实施

把已确认决策同步到 README 和项目设计/采集文档：新导航名、标准 SKU 聚合、模型/订阅计划→卡网/中转站关联、模型排行与视频生成占位、MySQL 唯一运行时、示范数据的非实时属性、采集详细/展示精简的分层、Python 本机/可迁移工具、来源优先级、配置默认值（false/60/1000/0）、`site.score=50` 的展示初值、站名去重与同级最低价。明确分开“本轮已实现”和“等待用户逐来源批准”：真实 HTTP 采集、Windows/远程调度、staging 发布到公开快照。说明现有 `partnership`/提交入口继续沿用，本轮不改表单、不把表单结果自动联网或自动公开发布；后续可由人工把已审核提交登记为 source registry 条目。不得写入任何密码、服务器登录信息、SQL dump 或完整第三方响应。

## 验收

- README 和设计文档相互不冲突；所有路径、路由和端口与当前项目事实一致。
- 文档可以使下一位 AI 在不猜测权限边界的情况下继续；公开/私密信息边界已审查。
