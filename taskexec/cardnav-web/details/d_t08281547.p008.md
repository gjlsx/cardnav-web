# p008 端到端验证与独立站发布

## 实施

先完成全部本地回归：MySQL 种子/聚合/关联查询计数、模型排行（含视频生成空态）、Python fixture staging、`pnpm test`、`pnpm run typecheck`、`pnpm run build`；再用浏览器从首页进入目标页面，验证 desktop/mobile 和 zh/en/ru。至少完成一个模型或官方计划的内部链路：计划/模型 → 聚合卡网 → 按评分排序的支持中转站。通过既有 `D:\phpStudy\WWW\likeshop\howtorunvpsnew\.md` 中已提供的安全连接方式发布独立站到 `ai.lovemoney.live`，仅更新该站 Node 构建/服务和其 MySQL 样例数据。

## 验收

- 生产 HTTP 80 与 HTTPS 目标页面响应正常，`/shops`、`/llm-gateway`、`/official-price`、`/model-leaderboard`、`/guide` 的主要结果可见，且关联内部链路可走通。
- `http://dtch.yg2022.top:8086/shop/`、`http://dtch.yg2022.top:8090/mobile/`、`http://dtch.yg2022.top:8095/admin/` 均仍返回可用响应。
- 服务器不运行 Python collector、没有安装/启用任何采集 schedule；凭据不落入 git/tasklog。
