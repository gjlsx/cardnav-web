# p009 项目根目录 VPS 发布指南

## 实施

新增 `D:\work\dock\cardnav-web\howtorunvpsnew.md`，结构遵循本机标准来源 `D:\phpStudy\WWW\likeshop\howtorunvpsnew.md` 的文档分工、固定要求、发布/恢复本地调试、验证命令、浏览器入口、端口和注意事项。它必须是本项目专用的可复用发布记录，而不是 LikeShop 文档副本。

仅写入本项目事实：本地构建与 MySQL `ailovemoney` 样例同步、目标独立目录/服务的安全引用、Apache 80/443 反代边界、`ai.lovemoney.live` 的 HTTP/HTTPS 验证，以及不干扰 LikeShop `8086`、`8090`、`8095` 的检查。连接用户名、密码、私钥、导出文件路径中的秘密内容不得复制；只说明从本机受控安全文件读取。记录本次实际发布命令时也必须脱敏。

## 验收

- 项目根目录存在 `howtorunvpsnew.md`，与本机标准文档有一致的流程章节，但内容仅针对 cardnav-web。
- 文档包含构建、样例数据库、独立服务、HTTP 80/HTTPS、LikeShop 隔离验证及回滚/恢复本地调试边界。
- 对文档做凭据扫描，不含密码、SSH 私钥、token、cookie、数据库连接串或 SQL dump；路径和端口与实际部署一致。
