# p010 展示收口、收录联系入口与采集器验收

## 范围

根据 2026-08-28 最新验收意见，对已完成 p002/p003/p005/p006 做最小回归修复：

1. 中转网站公开列表移除“模型数”“支付方式”列与支付方式筛选；保留模型筛选、评分、支持模型、样例提示和采样时间。
2. 中转站收录入口增加人工邮件提交卡片：邮件 `xiu.juan2love@gmail.com`、Telegram `https://t.me/+AX9TXrzMaS04OWI1`，提供可复制的资料清单与 `mailto:` 草稿；仍保留既有站内提交审核路径。文案参考用户提供的 PriceAI 截图的信息层级，但不复制其文案或品牌。
3. 中转站列表和详情恢复“打开”按钮。目的地址为数据库 `gateway_sites.url`，本轮样例种子可以固定填入已核验公开站点 URL；没有 URL 时不渲染按钮，不生成空或 `null` 链接。下一轮采集/配置工具将管理此字段，不在本轮增设配置系统。
4. 卡网商品公开展示不显示“来源：……公开渠道……参考”等来源文字；保留非实时/参考样例状态。模型排行榜的可见页面标题统一为“模型排行”（英俄同步），不显示 `CardNav 公开模型排行参考` 之类来源行；底层溯源数据仍保留。
5. 对 `scripts/collection` 完成度进行代码与运行验收：确认 CLI、GUI、fixture dry-run、staging 写入、默认无调度/无真实 HTTP 的边界；启动本机 GUI 留给用户人工检查，并在 QA 中记录启动命令、监听/进程状态和任何明确缺口。

## 验收

- 三语页面在桌面/移动端显示列和文案一致；首页导航进入中转页后能完成邮件/Telegram 联系、详情和有 URL 的打开动作。
- 无购买行为自动化、无调度安装、无未批准真实抓取；公共页面不泄露凭据。
- 对本机 GUI 做可见启动检查，CLI 测试、项目 TypeScript 测试、类型检查和构建通过。
