# p019 端到端验收、文档和必要收口

## Files

- Modify: `scripts/collection/README.md`
- Modify: `README.md`
- Modify: `howtorunvpsnew.md`
- Create: `taskexec/cardnav-web/docs/qa/p0_codex_t08281547.p019.md`
- Test: `scripts/collection/tests/test_collection.py`

## Work

完成本机 MySQL schema/fixture 种子、Python collection 测试、`pnpm test`、`pnpm run typecheck`、`pnpm run build`。从真实桌面入口运行 `python scripts/collection/gui.py`，逐一打开三个根 Tab 和四个采集子 Tab，完成一条来源配置保存、一次本地 HTTP fixture server 的真实 raw→`collection_staging_observations`→本机 MySQL 正式发布、一次手工编辑/隐藏/解除后重新采集以及一次取消的危险运维确认。再启动本地 Astro，验证现有公开页面从 MySQL 读到受控正式数据且路由不变。

更新运行说明：GUI 不是生产采集器；说明首次启动、`.env`、默认调度、手工覆盖、raw 保留、恢复规则、如何安全退出。对 p011–p018 作要求/测试/重复逻辑/命名/过度设计检查，只修复本任务发现的必要问题；记录所有未执行的真实来源和生产发布边界。

## Acceptance

- QA 报告含实际命令、结果、GUI 入口证据、数据库检查和网页读取证据；不含秘密。
- 所有既有公共站测试仍通过；不新增对未批准外站的请求。
- 无未解释的 test failure、没有留下弃用的旧单页 GUI 入口或重复数据写入路径。
