# p020 本机 HTTP 验收资源回收

## 范围

`LiveMysqlHttpTests.test_approved_local_http_ingests_to_local_mysql` 结束本机 HTTP fixture server 时，除 `shutdown()` 外必须调用 `server_close()` 并等待其服务线程退出。仅修改测试资源管理；不改变真实 HTTP、MySQL 或发布行为。

## 验收

- 使用 `ResourceWarning` 严格模式运行该单测时不再产生未关闭 socket 警告。
- 完整 Python collection 测试仍通过。
