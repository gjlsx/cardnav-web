# p027 Collection workspace: batch capture, merge review, and formal publish

## Fix sources

`FIX:t08281547.p015,t08281547.p018,t08281547.p023`

## Work

Keep the approved desktop form: root tabs `采集数据 / 网站配置 / 合作运维`; child tabs `中转网站 / 卡网商品 / 官方网站 / 模型排行`; each child keeps its own saved activity log. Preserve the source-specific `抓取一次 / 开始循环抓取 / 停止 / 测试` controls and stop flag from p023.

Correct the capture workspace so it is understandable and safe:

- The left source list supports selecting several sources. `抓取选中来源（同一批次）` creates one batch and captures every selected source into it. A single-source action creates a one-source batch. Source test calls only the registered fixture `test.py`, shows command/stdout/stderr, and never sends real HTTP.
- Source loop/stop controls apply only to the chosen source(s), set/clear a visible run flag, and each loop iteration only creates a pending local batch. It must never publish formal data. A stopped loop logs `task <source> is stop!` and exits.
- The center/right area shows editable source configuration and the selected batch summary. Below it, provide clearly labelled views for source/run, unified raw audit records, legacy staging inspection, merge preview/conflicts, and current runtime records. Every list is filtered by the current child entity type and can refresh after a task finishes.
- Add `合并并入库运行数据表` for the selected completed batch. The normal confirmation dialog names the batch and raw count; confirming invokes p026, whose runtime-table transaction is publication. Capture screens visibly say `原始数据已入库，待合并` until this action succeeds.
- Keep precise record edit/delete/manual lock/hide/cancel-lock controls. A manual record is marked as such at the affected site/record key and is excluded from automatic merge/import until the user cancels the mark and re-runs merge; its original raw records remain retained.
- Do not hardcode external opening URLs in buttons: use the stored approved site URL, disable the action when it is absent, and log why.

## Acceptance

- Desktop-entry test shows a multi-select batch with at least two fixture sources, unified raw results, merge preview, and no runtime update until the explicit merge/import button is confirmed.
- The runtime record list changes only after that button; the public local website observes the new complete snapshot.
- Test button, loop start/stop, and each of the four independent tab logs are visually and programmatically verified.
