# p028 Corrected end-to-end acceptance and tasklist reconciliation

## Fix sources

`FIX:t08281547.p019`

## Work

Run the corrected local end-to-end flow through the real desktop entry point and local MySQL, then perform focused design review/refactoring only where evidence requires it. Validate all four collection child tabs and the three root tabs; include a two-source duplicate-key fixture batch, source priority, equal-priority price tie, lower-priority null-field fill, manual lock/hide, stop flag, raw retention, pending merge, explicit publish, and downstream Astro public snapshot read.

Record the exact code/test/database/browser evidence in a new task QA report and update current handoff docs. Reconcile p001–p023 by pointing to the correction tasks; do not change any existing `done` task status or manufacture a retrospective pass for p019. Keep p008/p022 production deployment partial and independent.

## Acceptance

- Python collection suite, strict warning mode, `pnpm test`, `pnpm run typecheck`, and build pass.
- The desktop GUI is verified from its actual launch entry, including the explicit `入库实际运行数据表` action and downstream formal/public result.
- The QA report identifies tested local DB state without secrets and lists any remaining real-source/server work as pending rather than claiming it complete.
