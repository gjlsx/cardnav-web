# p031 全量需求审计与遗留直发发布入口收口

FIX:`t08281547.p024`、`t08281547.p026`、`t08281547.p028`。

## 背景与原始需求基线

本任务不是重新定义业务范围。它以 `details/d_t08281547.overall.md` 记录的原始范围，以及 p024–p028 已确认的最终纠偏结论为准：多个来源先写本机 MySQL 的统一 `collection_raw_records`；随后由人工或独立启用的 `merge_import_batch(batch_id)` 跨来源合并、按来源优先级和同级最低价去重，并在一个事务写入运行时表/快照；该入库即是数据发布。

`site.score=50` 仅为前端站点排序初值，不得参与来源合并。`collection_staging_observations` 只保留历史兼容/查看；不得新增第二张 staging 表，也不得让新采集写它。手工锁定/隐藏不阻止 raw 审计保留，但必须阻止对应 stable key 进入 merge/import。

## 要做的最小修正

1. 审计 Python 采集库、CLI 和 GUI，确保采集器不存在绕开 `merge_import_batch` 的可调用直发入口；网站配置 Tab 的单条手工编辑保持为独立、显式人工写入例外。
2. 删除已废弃的 `PublicPublisher.publish_run()` 及仅服务于该路径的 repository 方法；保留仅用于受限历史恢复的读取/快照重建能力。
3. 在原始 overall 文档顶部增加简短的“当前权威数据流”提示，明确 p024–p028 与 `docs/collection-data-lifecycle.md` 覆盖其中旧的 staging 表述，保留原始文字作为需求历史，不篡改已完成任务。
4. 补最小回归，证明发布器没有 legacy `publish_run`，采集批次不能直接写运行时数据，批量自动数据只通过显式 `merge_import_batch` 入库。

## 明确不做

- 不重写已完成行，不新建数据格式或 staging 表，不清理已有本机数据。
- 不发真实第三方 HTTP，不安装调度，不改服务器，不重试 p008/p022 SSH 发布。
- 不将本机验证说成已完成生产发布；p008/p022 继续保留 `partial`，直至服务器 SSH banner 可用并完成受限发布与验收。

## 验收

- `python -W error::ResourceWarning -m unittest discover -s scripts/collection/tests -q` 通过。
- `pnpm test`、`pnpm typecheck`、`pnpm build` 通过。
- `rg` 证明没有生产可调用的 `publish_run`；当前数据生命周期文档与 overall 顶部指向相同唯一规则。
- 全量审计报告按：满足项、最小修正、剩余外部阻塞和无须实施的过设计项分类记录。
