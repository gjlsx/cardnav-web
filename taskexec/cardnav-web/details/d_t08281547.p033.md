# p033 AI LoveMoney 受限生产发布重试

FIX:`t08281547.p008`、`t08281547.p022`。用户于 2026-08-29 明确授权按项目根目录 `howtorunvpsnew.md` 发布服务器。

## 执行范围

1. 先以无认证 banner 探测确认 `206.119.177.74:22` 已正常提供 SSH；未恢复则不上传、不修改远端，并记录 `partial`。
2. 本机运行发布前回归：`pnpm test`、`pnpm typecheck`、`pnpm build`；仅导出最小、脱敏的本机 `ailovemoney` 运行时样例数据到临时目录，绝不提交 SQL 或秘密。
3. 从既有本机受控安全文件读取连接材料；不得把用户名、密码、私钥、连接串或 dump 内容写入仓库、tasklog、QA 或聊天。
4. 远端仅操作 `/www/wwwroot/ai.lovemoney.live`、其备份目录、服务器本地 `ailovemoney`、`ai-lovemoney.service` 和该站 Apache vhost。禁止改 LikeShop `8086/8090/8095` 的配置/进程；禁止生产采集器或调度器。
5. 发布后验证 `ai.lovemoney.live` 的 HTTP 80/HTTPS 443、主要页面、LikeShop 三入口、Apache 与 ai-lovemoney 服务状态、回环 3101、且无生产 collector/schedule。

## 验收

- 发布后真实域名或 origin Host-header 确认本站响应；浏览器可见首页、`/shops`、`/llm-gateway`、`/official-price`、`/model-leaderboard`、`/guide`。
- LikeShop 的 `8086/shop`、`8090/mobile`、`8095/admin` 均仍返回可用响应。
- 任何 SSH/banner/认证失败均为外部 `partial`，不得制造上传或成功记录。
