# p035 恢复俄文 README、受限发布与远程 MySQL 白名单

FIX:`t08281547.p034`。用户明确授权恢复工作区中被删除的 `README.ru.md` 到当前 Git 已提交版本，并立即继续生产发布；同时为远程 MySQL 开放访问。用户提供了两次相同 IP `216.144.231.55`，本任务按一个唯一 IPv4 白名单处理。

## 范围

1. 仅恢复 `README.ru.md`；保留其它未提交变更，尤其 `README.en.md`。
2. 重新执行本机 seed、测试、typecheck、build。测试通过后按 `howtorunvpsnew.md` 备份、发布本独立站、最小导入运行时样例数据、重启 `ai-lovemoney.service`，并验证 HTTP/HTTPS、LikeShop 三端口和无生产 collector。
3. 在服务器上创建或更新专用远程账户，仅授权 `ailovemoney.*`，host 限制为 `216.144.231.55`；不暴露 root，不授予 `*.*`，不添加第二个重复 host。
4. MySQL 必须只监听服务器需要的接口，防火墙仅对该 IP 放行 3306；其余来源拒绝。远程账号的强随机凭据仅写入本机忽略的 `.env` 的 `MYSQL_REMOTE_*`，不得进入 Git、日志或聊天。验证目标 IP 的连接语法与服务器 GRANT/监听/防火墙状态，且不回显密码。

## 验收

- 全量本机回归通过，且 `README.ru.md` 恢复后无文件缺失。
- 新构建和最小数据成功进入独立站；主要站点路由、80/443、LikeShop 8086/8090/8095 均可用。
- `SHOW GRANTS`、MySQL listener 与防火墙规则均只体现一个允许 IP；`root` 不接受远程访问。
