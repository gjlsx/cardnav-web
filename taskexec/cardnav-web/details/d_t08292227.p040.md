# t08292227.p040：根目录 AGENTS 通用护栏与本地入口补强

## 目标

在根 `AGENTS.md` 补充用户认可的通用行为护栏、本地环境、Astro 前端验证与文件引用规则；所有地址和边界必须以本仓库实际文件为准。

## 已核验事实

- 本地网站入口以项目 README 和 Astro 管理器为准，当前是 `http://127.0.0.1:3101/`；`pnpm run dev` 不固定假设为 4321，验证前使用 `pnpm exec astro dev status` 确认。
- 本机 MySQL 默认是 `127.0.0.1:3306`、数据库 `ailovemoney`，实际连接只从受控 `.env` 的 `MYSQL_*` 读取。
- 本机采集总控台是 `python scripts/collection/gui.py`，项目没有 NocoDB 管理后台。
- 用户已明确批准初始化 `docs/CURRENT_STATUS.md`。任务状态、断点与验证证据仍以 `taskexec/cardnav-web/` 为准；该状态文件只汇总可续接的材料状态、入口、验证、阻塞与 `Next TODO`。

## 要写入的规则

1. 以简洁形式重申全局 `q:` 只问答、完成/介入提醒、先给简要计划、极简和精确改动原则，避免与全局 AGENTS 重复一整份实现细节。
2. 内部代码证据使用绝对 Windows 路径和可选行号；不伪造 URL/URI 式本地路径。面向用户的最终回复仍服从平台对可点击绝对文件链接的要求。
3. 明确本地运行目标、采集 GUI、MySQL 默认端口和“先核验状态再访问”的要求；禁止把不存在的 4321 或 NocoDB 写为当前事实。
4. 明确 Astro/Node 公开站只读运行时表/快照，不在浏览器路由发起采集或写入；前端改动必须有针对性 `typecheck`/`test`/`build` 和必要的实际本地浏览器入口证据。
5. 明确 taskexec 作为任务状态与详细证据的权威地点；初始化 `docs/CURRENT_STATUS.md`，并以其作为简洁的材料交接和下一步建议。

## 验收

- 根 AGENTS 的新增规则与上述实际入口一致，且没有 NocoDB、4321（仅允许否定提醒）、其他仓库端口/协议或与平台最终回复格式相冲突的禁止项。
- p038 依赖本任务，防止新采集实现绕开补强后的规则。
- 新 `docs/CURRENT_STATUS.md` 有完整 frontmatter 与明确 `Next TODO`；用 `git diff --check`、关键词和排除项搜索验证。
