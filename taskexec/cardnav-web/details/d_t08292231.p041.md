# t08292231.p041：根 AGENTS 端点与提醒规则收口

## 用户确认

- 根 `AGENTS.md` 不出现 NocoDB。
- 本地 Astro/Node 入口只写已核验的 `http://127.0.0.1:3101/`，不提 4321。
- 保留并落实此前建议的通用护栏、绝对路径内部引用、前端/CLI 验证和 CURRENT_STATUS 交接。
- 补充可执行的完成/介入提醒命令；它与全局规则一致，但不复制延迟提醒的实现细节。

## 验收

- 根 AGENTS 含 `3101`，不含 `4321` 或 `NocoDB`。
- 根 AGENTS 的提醒规则使用标准 PowerShell beep 命令；q:、计划、极简、内部绝对路径、Astro/CLI、MySQL 和状态交接规则仍保留。
- p038 依赖本任务；仅文档变更，执行 diff/关键词验证。
