# t08292253.p001 独立采集程序骨架与来源登记

## 范围

创建新的独立 package（建议 `scripts/crawlee_collection/`），不重写或删除 `scripts/collection/`。集中来源登记必须只含总体要求中的三个 PriceAI 精确 URL，提供不可绕过的 allowlist 校验、`max_concurrency=1` 和 Crawlee `PlaywrightCrawler` 适配边界。旧采集 README 应说明 legacy 状态与新程序路径，但不能删除旧入口。

## 测试与验收

先写测试，确认来源 ID、URL、页面类型、数据类映射、禁用任意 URL 和并发限制。验证依赖导入与最小 CLI 配置检查；不在本任务进行真实网络请求。
