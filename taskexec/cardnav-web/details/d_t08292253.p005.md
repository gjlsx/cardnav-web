# t08292253.p005 PriceAI 本机真实采集入库与公开读取验收

## 前置条件

所有前序任务为 done，自动化测试通过，并从根 `.env` 安全读取本机 MySQL 连接。运行受管 Astro 检查后，手工使用新 CLI 采集三个精确允许的 PriceAI 页面；不访问其中任何外链。

## 验收

确认每个来源有 raw 批次，运行独立 `merge-once` 或显式 worker 的单次处理，确认相应运行库和快照已更新。通过 `http://127.0.0.1:3101/` 的卡网订阅、官方 API、中转 API 页面检查已读取数据或清楚记录真实页面结构无法可靠解析的部分结果。报告只记录 count、source id、batch id、时间和脱敏错误摘要，不记录 raw 正文、cookie、MySQL 凭据。
