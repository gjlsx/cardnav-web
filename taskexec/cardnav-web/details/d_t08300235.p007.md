# t08300235.p007 CardNav 中转站本机真实采集、入库与页面验收

## 前置与执行范围

`t08300235.p006` 已完成，全部自动化通过；读取仓库根 `.env` 的本机 MySQL 配置。仅手动启动新的 Crawlee CLI，采集 PriceAI 标准模型页、CardNav 站点列表和其 DOM 发现的同域一层详情页。不得访问任何站点对外链接或使用登录、Cookie、验证码、挑战绕过。

## 验收

1. 记录脱敏后的来源 ID、开始/结束时间、批次 ID、发现详情数、有效站点数、白名单模型覆盖数、价格记录数和失败摘要；不记录完整 HTML、Cookie 或数据库连接信息。
2. 对 raw 完成 batch 明确执行 merge/import，确认运行时 `gateway_sites`、`gateway_model_coverage`、`gateway_model_prices` 与 `gateway-sites` / `gateway-models` 快照有同批真实数据；只检查计数和无敏感样本。
3. 先确认 `pnpm exec astro dev status`，再通过本地 `http://127.0.0.1:3101/llm-gateway` 列表和一个本次采集的详情页验证站点、标准模型和可见价格读取。浏览器页面结果与数据库/快照一致才可标记 done。

