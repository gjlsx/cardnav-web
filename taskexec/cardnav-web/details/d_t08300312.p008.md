# t08300312.p008 3101 真实浏览器截图验收

## 目标

本任务的最终验收证据必须来自真实本地网站 `http://127.0.0.1:3101/` 的浏览器截图，而非 CLI、DOM 文本或数据库查询。

## 验收条件

1. 浏览器重新加载 `/llm-gateway`，截图中可见中转站总数、价格数据站点数与模型数。
2. 从本次采集站点进入一个实际详情页，截图中可见该站点、标准模型 ID 和至少一条公开价格。
3. 截图保存于 task QA 目录并在报告中以相对路径记录；若上述任一项失败，任务保持 doing 并继续排查，不得宣告完成。

## 范围限制

仅读取本机 `127.0.0.1:3101`，不提交表单、不访问商家外链、不改动该既存服务或数据库。
