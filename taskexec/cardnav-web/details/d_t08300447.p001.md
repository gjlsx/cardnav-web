# t08300447.p001 后备采集源合同与重叠策略锁定

把 `details/d_t08300447.overall.md` 的头脑风暴收成可执行来源合同，仍不发起真实采集。

## 必须锁定

- 精确 allowlist URL 与 source_id（含 GitHub 是 README 还是 `data.json`）。
- 禾维 `/sites` 与 GitHub 排行的主源/镜像关系和 priority。
- aiapipk「评分」实际是价格倍率时的字段映射；禁止 `site.score=0.6`。
- `site.score` 采用 overall 建议 A 或 B，并写出换算与「不覆盖手工分 / 不覆盖 region、benefit_text」。
- 模型套餐价格对比的**精确同域 URL**；没有则记 gap，不得跟商家外链。
- 是否允许同域 `/sites/{slug}`；若允许，路径规则写死在代码合同，不能由 config 加 URL。
- robots/条款结论：通过才可在后续任务标记 approved。

## 验收

- 合同写进本 Detail 的补充结论或 `docs/` 既有采集文档的最小补丁，不新建平行架构文。
- 带测试的常量：priority、24 小时新鲜度、score 换算（若选 A 则 `rating*20` 等）。
- 本任务结束时仍无真实 HTTP 采集。
