# t08300447.p002 登记后备采集源到 Crawlee 白名单

按 p001 合同，把新 source_id 和精确 URL 硬编码进 `scripts/crawlee_collection/sources.py`，并让 `catch.config` / example 能开关这些已有 id。

## 验收

- 测试：未知 URL、config 加站外 URL 一律失败。
- 默认 `enabled=false`；`max_concurrency=1` 不变。
- 新 CLI 枚举能看到新源；legacy `scripts/collection/` 不被调用。
- 不实现页面解析。
