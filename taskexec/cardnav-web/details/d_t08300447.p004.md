# t08300447.p004 禾维 GitHub 实时排行解析

解析 p001 选定的 GitHub 精确 URL（README 表或 `data.json`）。

## 验收

- 字段：排名、站名、在线率、延迟、模型数、用户评分等进入既有 GATEWAY_SITE / 性能字段白名单。
- 与禾维 `/sites` 使用同一稳定键，便于 p006 去重。
- fixture 测试；不跟随 README 里的商家或 `hvoyai.com/sites/{slug}` 下游，除非 p001 已把同域详情写成代码规则。
