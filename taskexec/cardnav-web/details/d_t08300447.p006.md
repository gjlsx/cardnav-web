# t08300447.p006 三源与既有站去重合并

把 aiapipk、GitHub 镜像、禾维综合榜与已有 PriceAI/CardNav 运行时行按 host 合并。执行 p001 的 priority 与 24 小时新鲜度；不覆盖 `region`/`benefit_text` 和手工锁。

## 验收

- 测试：同源镜像不产生双行；低优先级不覆盖高优先级有效分；空字段可补。
- score 换算与 p001 一致，并断言 `openai 0.6` 不会变成 score 0.6。
