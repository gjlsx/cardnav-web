# t08300447.p007 后备源本机真实采集与 3101 读取

仅在 p001–p006 自动化通过后，本机对精确 allowlist 手动采集 raw，merge/import 一次，再从 `http://127.0.0.1:3101/llm-gateway` 确认读取。

## 验收

- 先 `pnpm exec astro dev status`。
- 记录批次 id 和写入条数，不写 raw 正文、Cookie、密钥。
- 列表排序仍按本站规则；截图或等价浏览器证据证明有后备源字段进入展示（评分/刷新/站点），且无商家外链被爬取。
- 任一源 robots/挑战失败则该源 `partial`，不绕过。
