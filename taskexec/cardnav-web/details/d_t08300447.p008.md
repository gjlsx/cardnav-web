# t08300447.p008 首页公告可编辑发布

当前公告只在 `src/i18n/zh.ts` 的 `announcement.message`，控制台「网站配置」不能改。本任务提供本机可编辑并发布到公开页的路径。

## 产品

- 中文默认文案：`欢迎大家来到 AIGate 欢迎大家进群交流意见` + QQ 群链接。
- QQ 使用 `src/site.ts` 的 `qqGroupNumber=1106704568`；无加群 URL 时不得编造邀请码。
- 「AIGate」仅公告用语，不改整站品牌。
- 覆盖 0828「公告纯文本无链接」：允许这一处 QQ 群链接。
- 最小路径：总控台网站配置（或同等一条 CLI）写入 `public_snapshot_entries`（建议 key `homepage-announcement`），`PublicPage` 优先读快照。

## 验收

- 不改 i18n 也能通过控制台/CLI 改公告并在下一请求的 3101 首页看到。
- 自动化测试覆盖快照优先、QQ 群号来自 `site.ts`、旧「纯 span 无链接」断言已更新。
- 本任务不做浏览器截图（留给 p009）。
