# t08300447.p011 GitHub 中转参考 raw→快照适配器

## 工作

使用 `scripts/crawlee_collection/` 既有 CLI 与 raw 格式接入 p010 的数据。采集适配器只写 raw payload/records；同一既有 merge/import 事务才可更新 `gateway_sites` 与公开快照。`source_rank`、上游更新时间和上游评分仅作为来源参考字段，手工 `region`/`benefit_text`、手工锁和本站 `site.score` 不得被覆盖。

## 验收

- fixture 覆盖来源字段、空值、重复 host 和稳定键；失败不触发 merge/import。
- 现有快照读取到来源说明、上游参考排名和采集时间；不创建第二个业务 schema或直接运行时写路径。
- Python/Crawlee 相关测试通过。
