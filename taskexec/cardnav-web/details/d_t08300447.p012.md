# t08300447.p012 GitHub 8 小时本机采集控制

## 工作

在唯一 `catch.config` 里为 `hvoyai-awesome-ai-api` 声明 `interval_minutes=480`、`enabled=false` 和既有条数/并发限制；控制台 GUI 只对这项已登记来源提供抓取一次、开始循环、停止与配置编辑。循环只在本机 GUI/worker 存活时运行，采集只写 raw；merge/import 仍由同一个默认关闭的单例流程处理完成批次。

## 验收

- 配置读取、边界校验与 GUI 命令测试覆盖 480、非法值和默认关闭。
- 没有 Windows Task Scheduler、cron、服务安装或生产采集代码。
- 手工运行/循环调用同一 CLI，不允许配置任意 URL。
