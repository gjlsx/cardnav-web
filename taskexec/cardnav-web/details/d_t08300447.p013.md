# t08300447.p013 排名参考入口与中转参考跳转

## 产品约定

- 新公开入口 `/ranking-reference`，总名“排名参考”。旧 `/model-leaderboard` 兼容并重定向/映射到“模型表现”页签。
- 页签：`模型表现`（既有任务表现）、`中转参考`（GitHub 禾维站点参考）和后续的 `价格参考`。
- `/llm-gateway` 增加站内“查看中转参考”按钮；不删除现有中转站主体列表，不向外部网站跳转。

## 验收

- 路由、三语显示文案、SEO/canonical 和首页入口一致；旧模型详情深链接仍可用。
- 组件/路由测试覆盖旧路径兼容、页签目标和中转页站内 CTA。
- 浏览器验收留给 p017。
