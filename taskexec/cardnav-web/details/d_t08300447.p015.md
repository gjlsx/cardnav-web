# t08300447.p015 价格参考页签

## 工作

只使用既有 `gateway_model_prices` 的公开数值建立“排名参考 > 价格参考”。模型名必须使用项目标准 ID；按 `unit`、价格方向（输入/输出/缓存）分别比较，同一行展示最低公开报价、覆盖站点数、单位和最近采集时间。不同单位、图片/视频与 token 价格不可混合排行。

## 验收

- SQL/快照与测试证明只纳入可比较、数值有效的价格。
- GitHub 中转参考数据不参与价格排行；倍率、宣传语和空值不参与排序。
- 空态和数据来源提示对用户可见。
