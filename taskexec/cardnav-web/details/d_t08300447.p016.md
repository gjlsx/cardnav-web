# t08300447.p016 AIGATE 双域品牌适配

## 工作

将所有用户可见的 AI LoveMoney 品牌替换为 `AIGATE`，新主域为 `aigate.live`。在一个代码库中支持新旧 host：新域为 canonical，旧 `ai.lovemoney.live` 保留路径兼容并转向新域。同步公开页面、导航、移动端、SEO、结构化数据、README、站点常量与公开链接；不篡改采集 raw/历史来源名。

## 验收

- 源码测试覆盖两 host 的 pathname 保留、canonical、重定向和用户可见品牌；不误替换第三方来源事实。
- 本机 host/路由验证通过。DNS/Apache 生产配置仅在单独发布授权下执行。
