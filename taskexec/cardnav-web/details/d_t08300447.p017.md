# t08300447.p017 一次真实采集与 3101 全流程验收

## 工作

完成全部自动化后，本机按 p010 精确 allowlist 真实采集 GitHub `data.json` 一次，验证 raw→merge/import→公开快照；然后从 `http://127.0.0.1:3101/` 正常导航验证“排名参考”三个页签、中转网站 CTA、AIGATE 品牌、桌面/移动显示和一次 8 小时配置入口。

## 验收

- 报告只记录 batch/run、计数、采集/合并时间、来源 ID 和脱敏失败摘要，不写完整 raw 或秘密。
- 保存桌面/移动截图；页面数据与运行时/快照计数一致。
- `pnpm run typecheck`、`pnpm test`、相关 Python 测试及构建通过。任一真实采集/浏览器项未通过不得标记 done。
