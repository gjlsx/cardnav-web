# t09030447：全局回顶、允许来源数据刷新与 VPS 发布

## 用户需求

1. 所有公开页面增加便于长页返回顶部的入口；用户确认采用右下角固定悬浮按钮。
2. 更新当前已批准的采集数据，先写入本机 raw，再手工 merge/import 到本机运行库。
3. 本机验收后按根目录 `howtorunvpsnew.md` 更新服务器。

## 固定约束

- 按钮只放入共享 `PublicPage`，滚动超过一屏后可见；点击平滑回顶；不在 Guide 独立滚动阅读器强行插入。
- 只使用 `scripts/crawlee_collection/catch.config` 中固定 allowlist：PriceAI 卡网/官方/中转、CardNav 中转详情（含 PriceAI 标准模型依赖）、Hvoy 公开参考 JSON。
- 采集仅在本机运行，固定并发 1；不跟随外部商户链接、不处理登录/验证码/挑战，也不保存秘密或完整 raw 页面到仓库。
- `collect --all` 产生 raw 批次；仅在所有来源无失败时执行 `merge-once --batch`。采集失败不得清空既有运行时数据。
- 发布只操作独立 AIGATE 站点，保持 LikeShop 8086/8090/8095、远端 `.env` 和服务器无采集器/定时器。

## 验收

- 3101 长页滚动后通过按钮返回顶部，桌面浏览器截图留证。
- 真实采集/merge 记录 batch ID、统计和运行时读取结果，不写入秘密或 raw 正文。
- 发布后验证旧域 308、新域页面、源站 80/443、LikeShop 三端口和无生产采集任务。
