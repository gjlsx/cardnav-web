# p005 榜单到外跳的匿名会话链

Depends：t09100118.p003、t09100118.p004。

## Files / Interfaces

Create：src/scripts/measurement.js、test/measurement-browser.test.ts。
Modify：src/layouts/PublicPage.astro、src/components/ModelLeaderboardContent.astro、src/scripts/page-widgets.js、src/scripts/llm-gateway-home.js、src/scripts/gateway-detail-tables.js、src/pages/llm-gateway.astro、src/pages/llm-gateway/models/[modelId].astro、src/pages/llm-gateway/[slug].astro；必要新文案进现有三语消息文件。
隐私说明仅更新 content/pages/zh/privacy.md、content/pages/en/privacy.md、content/pages/ru/privacy.md 中本功能真实处理方式，不能替用户宣告已满足所有地区法律。

消费 p004 字段合同；浏览器 fetch('/api/measurement', ...) 不带完整原始对象。每个 DOM 语义动作只生成一次 eventId，重复发送必须复用它，不同时加 Umami 自动和手动同名事件。

## Steps / Events

- [ ] 先写失败测试（可复用项目 fake DOM 风格；不引入大测试框架）：disabled、不允许统计、sessionStorage 抛异常、离页失败、动态加载行、重复事件绑定。
- [ ] 服务端 enabled=false 时不注入该追踪脚本。开启后还须用户允许本会话统计；用现有页面风格的简短选择/撤销控件，默认未允许不发事件。允许状态仅存在 sessionStorage，拒绝/撤销清空本功能缓存并停止发送。
- [ ] 单个标签页使用 crypto.randomUUID() 建 sessionStorage 标识，跨同源导航保留。每次页面载入重置 event listener 状态，复用 sessionId；拒绝存储时不回退长期指纹、cookie 或 localStorage。
- [ ] 来源只映射白名单 utm_source 和已知搜索来源域为 channel；没有证据用 direct-or-unknown。第一入口 channel 在本会话保留，站内跳转不覆盖；不保存 UTM 任意文本、完整 referrer 或搜索词。
- [ ] 排名页可见且用户允许后发 ranking_view；点击证据链接发 evidence_open；进网关比较页发 comparison_enter；用户真正筛选/排序才发 comparison_interact（只报该事件，不报关键词）；点击现有明确外部目标发 outbound_click。只覆盖 Overall 页面族，不为全站所有点击埋点。
- [ ] 不发送 comparison_complete/qualified_referral：本单未建立合格可信比较判据。网关赞助混排尚未整改的行 placement=unknown，不能按 sponsor=false 就命名为独立自然榜；自营关系不明 targetKind=unknown。
- [ ] 不 preventDefault，不加自有跳转代理，不等待统计响应才跳转；使用 fetch keepalive 做尽力发送，失败不无限重试、不阻塞链接。新标签页/修饰键行为保持浏览器默认；不能声称跨标签事件一定可串联。
- [ ] UI 保留真实来源/分数/自然顺序；统计故障、许可撤回和禁用时页面可正常使用。
- [ ] pnpm exec tsx --test test/measurement-browser.test.ts test/measurement.test.ts；pnpm run typecheck。再通过 3101 正常入口检查用户操作 → 网络事件 → p004 下游本机聚合查询，synthetic 会话只用于 QA，不是真实受众。
- [ ] 自审避免泄露；记录采集同意/关闭行为和真实下游结果，再提交。

Done：明确的用户操作被尽力记录，重复监听不双计，拒绝统计用户不被跟踪，所有来源/数据缺失如实表达。
