# p007 整体验收与交接

Depends：t09100118.p001–p006 全部完成。必读 Overall 与各 QA。
Files：taskexec/cardnav-web/docs/qa/p1_codex_t09100118.p007.md、docs/CURRENT_STATUS.md；按实际结果最小更新 docs/ROADMAP.md 的本单状态，不另建战略。必要可在现有测试文件中补本单遗漏回归。

## Verification steps

- [ ] 复读 tasklist 及自己的 locks，确认依赖真实 done/提交证据完整；不能仅因前任务日志写了 done 直接跳过检查。
- [ ] 运行 pnpm run typecheck、pnpm test、pnpm run build。记录真实退出码；此前历史通过不能代替本轮结果。
- [ ] pnpm exec astro dev status 后，从 http://127.0.0.1:3101/ 正常导航进入排名参考，核对源名、原榜链接、原日期、样例/未知、真实零和缺失分数。桌面及移动、zh/en/ru 验证首屏与加载更多一致，原任务页签/URL 不退化。
- [ ] 用户允许本会话统计 → 查看榜单 → 查看来源 → 现有比较入口 → 网关/模型筛选 → 明确外跳动作。核对请求字段不含原 URL、关键词/身份，接口响应/本机统计查询/周报一致。使用浏览器拦截外部导航的方式验证目标行为，不提交商家订单；清楚记录未实际访问外站。
- [ ] 关闭统计、撤回许可、禁止 sessionStorage、接口 503、离页、重复点击、超长请求、异源请求、重复 eventId、超过日限额分别验证。链接和浏览必须不依赖统计成功。不能声称可测出所有被拦截事件。
- [ ] 没有真实榜数据或方法证据时核对降级；fixture 只用于隔离验证，不写用户运行目录。若真实数据链未取得，QA 明列缺口，不称为新来源接入通过。
- [ ] 回滚演练只在本机：关闭 MEASUREMENT_ENABLED 后不再发送/写入；保留已有统计表与数据，不用 DROP TABLE 清理。原商品/榜单/采集行为不变。
- [ ] 检查新增配置与硬编码：五个 MEASUREMENT_* 只在 config 读取，MAX_EXTERNAL_REPORT_BYTES 集中定义；没有评分权重、商业报价或增长百分比。报告须按服务端环境过滤，local-test 不得混入 production。检查密钥、原始正文、访客明细未进入 diff。
- [ ] 按真实结果写 QA；仅技术验收全部通过可 done。运行条件不满足就 partial/blocked，保留具体下一步，不以本机路由 200 替代 UI/下游证据。
- [ ] CURRENT_STATUS 按跨日期备份规则更新，记录本单结果/开关仍关闭、剩余真实来源/基线/全站隔离/自测任务；路线图里程碑仍未全部完成。
- [ ] 自审、实现提交、日志与状态 hash 回写、状态提交、移除自己的锁。若全 done 按 taskexec 归档并提交索引，结束前再检查无可运行本单任务。

## Handoff package

列明已验证/未验证和证据路径；下一份外部来源接入清单需用户确认精确来源/使用许可，再下一份自测需仓库 revision、公开测试数据与结果许可、执行目标/费用、参数和完整失败样本记录。本任务不触发新清单执行，不购买 API、不部署生产，不代发渠道内容。

p007 done 只关闭本地引用可信化与计量工程，不将 M0/M1 整体、自测飞轮或商业模式标记成功。
