# ISSUE-006 [P1] 计量任务归档后的代码复核

复核日期：2026-09-10。范围：`tasklist09100118` 中 p004–p006 的计量实现及其直接展示入口；不修改用户现有采集、部署和数据库说明改动。

## 发现与修复

| 级别 | 问题 | 修复 |
|---|---|---|
| P1 | 日配额按 UTC 切日，违背既定 `Asia/Hong_Kong` 口径；重复事件的并发唯一键冲突可能被当作 503 | 统一香港日键；日锁内使用幂等插入，重复不消耗配额。 |
| P1 | 已启用浏览器逻辑没有白名单渠道归因、比较页进入事件或明确外跳事件 | 仅保存白名单 UTM/来源域映射；为比较页和既有明确外跳添加受限事件属性。 |
| P1 | 周报缺外部 JSON 校验、顺序路径归约和保留期清理 | 增加 1 MiB 严格 JSON 合同、`enter → interact → outbound` 计数、默认 dry-run 清理和本机目标保护。 |
| P2 | 同意控件硬编码英文，隐私页未描述新功能 | 三语文案和真实的会话统计处理边界。 |

## 验收边界

- 仍默认关闭；本次不初始化表、不写本机数据库、不启用生产统计、不采集新来源或发布。
- 周报的 external metrics 仅来自用户明确给出的本地 JSON，标记为用户提供、未独立核验；不下载 URL。
- 浏览器离页发送仍是尽力而为，拒绝、撤销、拦截和容量限制会造成缺失，不能推断为全量受众或成交。
