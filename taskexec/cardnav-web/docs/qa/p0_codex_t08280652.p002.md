# p002 QA — MySQL 参考样例种子

- 时间：2026-08-28 07:01–07:02 Asia/Hong_Kong
- 变更：新增 `reference_data_sources`；为 `shop_products` 增加来源、标准产品、平台/类型、货币、渠道计数、采样时间和样例身份字段。迁移逻辑通过 information_schema 检查后再添加，兼容 MySQL 5.7。
- 种子：`pnpm run seed:reference-samples` 连续运行两次，结果稳定为 `sources=3`、`sites=7`、`products=9`；脚本只删除自身 `reference-*` 的 `is_sample` 商品和站点后重写，未触及其他数据。
- 快照：`shop-products` 快照为 9 条；运行时读取验证得到 `sites=7`、`products=9`、`visible=9`、`samples=9`、`sourced=9`。
- 回归：`pnpm run typecheck` 0 errors；`pnpm test` 66/66 passed。
- 自审：所有样例均来自受限的公开页面字段，未设置商品购买链接；没有写入秘密或外部导出；没有实现自动抓取或定时器。p003 仍需将来源/样例字段可见化并完成浏览器/生产验证。
