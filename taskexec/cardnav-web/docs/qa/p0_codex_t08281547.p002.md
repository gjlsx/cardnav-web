# p002 QA evidence — standard SKU aggregation

- Date: 2026-08-28 Asia/Hong_Kong
- Scope: canonical catalog and explicit relationships; `/shops` defaults to a non-transactional SKU aggregate, with merchant observations available only through an in-page detail control.

## Database and seed verification

- Ran `pnpm run seed:reference-samples` twice against local MySQL `ailovemoney`.
- Both runs returned the same result: 3 reference sources, 9 offers, 7 sites and only `shop-products` / `shop-products-packed` snapshots.
- The seed preserves the unrelated popular-search snapshot. Migration tests cover deterministic legacy duplicate removal before the new natural-key index.

## Automated verification

- `pnpm test` — 77 passing tests.
- `pnpm run typecheck` — passed without diagnostics.
- `pnpm run build` — passed.

## Browser verification

Fresh built `/shops` was checked on desktop and 342 px mobile:

- Default aggregate showed 8 canonical SKU rows with nonempty product names and contiguous price-sorted sequence `1` through `8`.
- Every row retained platform/type/source/sample, price, available/total channels and time; its local channel control toggled `aria-expanded` from false to true.
- The aggregate has no external merchant/product anchor. Mobile has no document-width overflow.
- Independent UI evaluation passed after three review iterations; independent code re-review approved the corrected same-site priority merge, no-external-link drill-down, and safe index migration.

Screenshot: `C:\Users\wind\AppData\Local\Temp\cardnav-p002-shops-mobile.png`.
