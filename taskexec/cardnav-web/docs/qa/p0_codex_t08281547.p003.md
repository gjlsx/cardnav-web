# p003 QA evidence — gateway reference samples

- Date: 2026-08-28 Asia/Hong_Kong
- Scope: public reference-only gateway catalog; no crawler, performance claim, purchase path, or invented outbound URL.

## Data verification

- Local MySQL sample seed was run twice. It remained idempotent and now writes `gateway-sites` and `gateway-models` alongside the existing shop snapshots.
- The reference catalog contains five score-50 sites. Model coverage is a separate relation from optional model prices; the GPT filter returns two supporting sample sites even with zero public price records.
- List and detail display the same Beijing-time observation timestamp.

## Verification

- `pnpm test` — 83 passing tests.
- `pnpm run typecheck` and `pnpm run build` — passed.
- Independent code re-review approved the stable score/name order, safe nullable URL handling, hidden unknown performance, provenance display, and snapshot boundaries.
- Independent browser evaluation passed desktop/mobile/home and zh/en/ru model filtering: explicit sample/source/time, no overflow, no external/null/open/invite/buy action, and no fabricated availability/latency.
