# p008 QA — 端到端验证与独立站发布

- Executor: grok
- Date: 2026-08-28
- Result: **partial**

## Local (complete)

| Check | Result |
|---|---|
| `pnpm test` | 91 passed |
| `pnpm run typecheck` | 0 errors |
| `pnpm run build` | server build complete, dist packed 2456871 bytes |
| seed counts | snapshots include model-leaderboards; leaderboard samples=12, official=9, shop=9 |

Browser `http://127.0.0.1:3101` from homepage:

- 模型排行 gpt-4o visible; 视频生成 → 暂无公开参考样例
- 官方网站 → 关联卡网（3 行）和 `/llm-gateway?model=gpt`
- `/guide`、mobile `/shops`、`/en/`、`/ru/` load

## Production host checks (current live process)

| URL | HTTP |
|---|---|
| https://ai.lovemoney.live/ | 200 |
| /shops /llm-gateway /official-price /model-leaderboard /guide | 200 |
| http://dtch.yg2022.top:8086/shop/ | 200 |
| http://dtch.yg2022.top:8090/mobile/ | 200 |
| http://dtch.yg2022.top:8095/admin/ | 200 |

LikeShop three ports remain reachable. Credentials were not written to git.

## Gap

Origin SSH on 206.119.177.74:22 accepts TCP then closes before the SSH banner (`ssh_exchange_identification: Connection closed by remote host`). Paramiko and Windows OpenSSH both fail. Dist and sample SQL were packed locally (`D:\temp\ailovemoney-p005\p008-dist.tar.gz`) but **not uploaded**. The live Node tree is still the previous release.

Retry after the origin again accepts SSH. Do not treat current production HTML as this round’s leaderboard/video-generation build.
