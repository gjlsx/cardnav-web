# p010 QA — 公开展示收口、收录联系入口与采集器验收

- Executor: codex
- Date: 2026-08-28

## 已落实

- 中转站公开列表只保留序号、基本信息、评分、支持模型和模型筛选；移除了模型数、支付方式列及支付筛选的客户端状态。
- 站内“提交中转站”弹窗保留原有审核表单，新增人工邮件收录：`xiu.juan2love@gmail.com`、带资料清单的 `mailto:` 草稿，以及已授权 Telegram 链接。
- 样例 URL 由 `referenceGatewaySamples` 写入 MySQL `gateway_sites.url` 和公共 snapshot；列表/详情只对非空 URL 生成“打开”。本轮已核验样例为 Genius Coder 与贝贝，其余 URL 仍为空。
- 卡网商品的渠道展开项只显示价格/库存，不显示来源站名称；模型排行页面标题统一为“模型排行”（英俄同步），不显示排行榜来源名称，底层来源数据仍保存。

## 自动验证

```text
pnpm test                         # 93 passed
pnpm run typecheck                # 0 errors / 0 warnings
pnpm run seed:reference-samples   # sources=5, products=9, sites=7
pnpm run build                    # passed

cd scripts/collection
python collect.py check-config    # 5 sources, no errors
python collect.py dry-run         # network_requests=0, raw=6, merged=3
python collect.py write-staging   # MySQL staging_count=3, published=false
python -m unittest tests.test_collection  # 9 passed
```

## 浏览器验收

本机构建服务 `http://127.0.0.1:4334`：

| 页面/动作 | 结果 |
|---|---|
| `/llm-gateway` | 未出现“模型数”或“支付方式”；Genius Coder、贝贝各有一个数据库 URL 驱动的“打开”链接 |
| 提交中转站弹窗 | 邮件 `mailto:`、资料清单、Telegram 链接均可见 |
| `/shops` | 未显示 PriceAI/OpenPrice 的公开渠道样例来源文字 |
| `/model-leaderboard` | 显示“模型排行”，未显示 `CardNav 公开模型排行参考` |

## 采集器人工检查

已在本机以可见窗口启动：

```text
cd D:\work\dock\cardnav-web\scripts\collection
D:\work\python\py310\python.exe gui.py
```

运行进程：`python` PID `19040`（启动时确认存活）。

当前完成度是已批准的 MVP：本机 CLI/GUI、配置校验、fixture dry-run、优先级/最低价合并与 MySQL staging 已完成；默认没有调度、没有真实 HTTP 抓取、没有自动公开发布。真实来源接入和定时启用仍须下一轮逐来源批准。
