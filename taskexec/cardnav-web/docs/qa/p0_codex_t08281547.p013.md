# p013 QA — raw → staging → public publish

- Executor: grok (user-authorized takeover of interrupted Codex p013)
- Date: 2026-08-29

## Codex already had

- process_batch raw retention + manual skip
- approved fetch exact allowlist only

## Completed in takeover

- PublicPublisher writes shop/gateway/official/leaderboard tables and rebuilds `public_snapshot_entries` in the same repository transaction
- CLI `write-staging` runs pipeline+publish against local MySQL
- Hidden/manual skip, publish rollback, local HTTP fixture ingest

## Commands

`python -m unittest tests.test_pipeline tests.test_fetch tests.test_collection tests.test_repository tests.test_contracts -v` — 28 passed

Includes live local HTTP server → allowlist fetch → `shop_products` + `shop-products` snapshot on MySQL `ailovemoney`.
