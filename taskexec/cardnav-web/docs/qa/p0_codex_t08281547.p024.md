# p024 documentation reconciliation verification

- Date: 2026-08-29
- Detail: `details/d_t08281547.p024.md`

## Checks

1. Read the original decision records and the active p011–p019 implementation contract.
2. Added `docs/collection-data-lifecycle.md` as the current source of truth: one unified source-neutral raw format, raw-first multi-source collection, separate merge/import, runtime-table write equals publication, manual lock behavior, source priorities, local-MVP boundary, and post-MVP remote-runtime-MySQL direction.
3. Updated the current README, navigation/collection fact document, collector README, and handoff document to link to the lifecycle. Marked superseded desktop-console specification, overall task detail, and brainstorm as historical so their legacy automatic-publish wording cannot be used as an implementation instruction.
4. Searched current entry documents for automatic-publish claims. The only matches are an explicit negation or an explicitly labelled historical phrase; no active instruction permits ordinary capture or a loop to write runtime data.
5. Confirmed the single-staging constraint remains explicit and no `collection_staging_records` term appears as a proposed table.

## Result

Pass. No runtime code, MySQL schema, source request, or public website data was changed by this documentation-only correction task.
