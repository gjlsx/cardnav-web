# p025 unified raw batch verification

- Date: 2026-08-29
- Detail: `details/d_t08281547.p025.md`

## Test-first evidence

`scripts/collection/tests/test_raw_batches.py` was added first. Its initial run failed because `SourceCapture` and `run_collection_batch` did not exist. After implementing the raw-only batch service, the two tests pass: a manual-locked key remains raw, and two sources share a batch while producing no staging/runtime write.

## Verification

- `python -W error::ResourceWarning -m unittest discover -s scripts/collection/tests -q` — 41 passed.
- `python scripts/collection/collect.py check-config` — five configured sources, no validation errors.
- `python scripts/collection/collect.py dry-run` — six unmerged source records, zero network requests for draft sources.
- `python scripts/collection/collect.py collect-raw` — one local fixture batch containing five source runs and six raw records; returned `runtime_writes: 0` and `published: 0`.
- Local MySQL scoped count after the command: five `raw_completed` batches, six raw rows with a batch id, and zero staging rows for the command's fixture runs.

## Result

Pass. Migration is additive: it adds batch/run/raw metadata and `collection_batches`, retains `collection_staging_observations` as the single legacy staging table, and does not create `collection_staging_records`. Capture no longer invokes `PublicPublisher` or updates runtime entities/snapshots.
