# p026 explicit merge/import verification

- Date: 2026-08-29
- Detail: `details/d_t08281547.p026.md`

## Test-first evidence

- `test_merge_import.py` initially failed because `collection_lib.runtime_import` did not exist. It now verifies API priority, lower-priority null-field fill, equal-priority lowest price, explicit import, idempotent retry, a post-raw manual lock, and runtime-write rollback.
- `test_legacy_recovery.py` initially failed because the recovery service did not exist. A later real local recovery exposed that a recovered staging row remained discoverable; a failing repeat-diagnosis test then verified the `recovered` state transition.

## Verification

- `python -W error::ResourceWarning -m unittest discover -s scripts/collection/tests -q` — 50 passed.
- `pnpm test` — 93 passed; `pnpm run typecheck` — 0 errors; `pnpm run build` — passed.
- Explicit local `merge-import` of the p025 fixture batch wrote three merged runtime records and marked that batch `imported`; a retry returns zero runtime writes.
- Scoped MySQL verification after bounded recovery: `collected-huasheng / chatgpt-basic-account` retains only the PriceAI winner at `0.3`; the lower-priority OpenPrice legacy row was backed up then removed.
- `legacy-report` now leaves three same-source legacy staging audit records as ambiguous and untouched. It removes only a legacy row when no current winner has that source, or when the current imported batch proves that source is a losing candidate.

## Result

Pass. `merge_import_batch` is the sole runtime-table/snapshot path and is transactional. Capture, loop, dry-run and source test remain raw-only. Legacy recovery is opt-in via `legacy-recover --confirm RECOVER_LEGACY`; it is provenance-bounded and never clears the database, samples, or ambiguous/current-winner rows.
