# p027 verification — batch capture workspace

## Scope

- Detail: `details/d_t08281547.p027.md`
- Entry: `python scripts/collection/gui.py` / `scripts/collection/console_app.py`
- Database: local MySQL only; no server or production deployment touched.

## Automated verification

- `python -W error::ResourceWarning -m unittest discover -s scripts/collection/tests -q`
  - Passed: 51 tests.
  - Includes independent-process desktop-entry smoke: three root tabs, four collection child tabs, multi-select source list, raw-audit/current-runtime views, explicit import button, per-page logs, source test and source stop flag.
- `git diff --check` passed.

## Desktop entry + local MySQL flow

From the real Tk console entry, selected two draft shop fixture sources and invoked `抓取选中来源（同一批次）`:

- batch: `batch-20260829035630-454046`
- raw records: 2
- before confirmation: batch state `raw_completed`; no capture-path runtime write.
- after the normal confirmation path for `合并并入库运行数据表`: batch state `imported`; runtime list contained 3 current shop rows.

The source action is raw-only, the UI visibly reports `原始数据已入库，待合并`, and only `RuntimeImporter.merge_import_batch` changes the runtime tables/snapshots. Manual lock/hide continues to target a selected raw stable key; it preserves raw history and prevents later automatic merge/import until cancellation. The approved-site opener is disabled unless a selected approved source has a stored `public_url`.

## Self-review

- Requirement satisfied: yes; sources can be multi-selected into one batch and there is no direct capture publication.
- Naming/duplication: `source_capture` centralizes fixture/approved HTTP construction; repository read models separate raw, legacy staging, and runtime rows.
- No further p027 refactor required. Future site branding/operations work is p029 and intentionally not mixed into this data-flow task.
