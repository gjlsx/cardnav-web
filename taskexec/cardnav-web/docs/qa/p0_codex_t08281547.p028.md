# p028 corrected local E2E acceptance and reconciliation

## Runtime boundary

- Scope: local MySQL `ailovemoney` and local Astro at `http://localhost:4321` only.
- No production server, real-source HTTP fetch, scheduler installation, or LikeShop service was touched.
- Current actual source fixtures are local draft fixtures. They demonstrate the data contract, not live commercial data.

## Desktop console entry and MySQL evidence

From the real `scripts/collection/gui.py` / `ConsoleApp` entry:

- Verified root tabs: `采集数据 / 网站配置 / 合作运维`; collection child tabs: `中转网站 / 卡网商品 / 官方网站 / 模型排行`.
- In `卡网商品`, selected `priceai-channels` and `openprice-products` together. Batch `batch-20260829040051-118922` retained raw rows from both sources, first reached `raw_completed`, then reached `imported` only after the confirmed `合并并入库运行数据表` action.
- Stable key `shop_product:huasheng:chatgpt-basic-account` retained both source observations. The local runtime winner was `priceai-channels` at `0.300000`, not the lower-priority OpenPrice `0.500000` value.
- Batch `batch-20260829040051-523374` was manually locked from its selected raw row before import. It was marked imported with its stable key skipped, leaving the existing runtime winner unchanged. After the mark was removed, batch `batch-20260829040105-361327` was merged again and the same PriceAI winner remained.
- A review-found edge case was corrected: a later single-source lower-priority loop batch now merges each affected stable key with the newest valid raw row from every source, preventing it from overwriting the higher-priority runtime winner. Regression coverage was added.
- Source stop/control and fixture-only test behavior remain covered by the strict desktop collection suite; each page uses a separate page log.

## Public local website entry check

- Started the local Astro development server and opened `http://localhost:4321/shops` in the in-app browser.
- Browser title: `卡网商品 - AI LoveMoney`.
- Observed the normal page entry/navigation and a populated standard-product table, including a `ChatGPT 普号` row at `¥0.3` and the runtime `gpt-4o` row. This confirms the local public snapshot read path remains populated after explicit merge/import.

## Full verification

- `python -W error::ResourceWarning -m unittest discover -s scripts/collection/tests -q`: **52 passed**.
- `pnpm test`: **93 passed**.
- `pnpm run typecheck`: **0 errors, 0 warnings, 0 hints**.
- `pnpm run build`: passed.
- `git diff --check`: passed.

## Historical task reconciliation

| Historical range | Corrected authority |
|---|---|
| p001–p005 | Kept: public navigation/catalog/sample work is independent of raw ingestion correction. |
| p006–p019, p023 | Superseded only for collection behavior by p024–p028; historical done rows are intentionally unchanged. |
| p020–p021 | Kept: test HTTP/data cleanup remains valid. |
| p022 / p008 | Still `partial`: production SSH/banner and deployment remain independent, not implied by local success. |

## Remaining explicitly pending work

- Approve and register any real HTTP source before network capture; no draft source may fetch a real site.
- Enable a scheduler only after a separate approval; scheduled capture and scheduled merge/import remain separate controls.
- Remote runtime MySQL direct write is MVP-after work requiring a separately approved endpoint/configuration task.
- p029 handles the already-approved sponsor, community-link, and Logo changes next.
