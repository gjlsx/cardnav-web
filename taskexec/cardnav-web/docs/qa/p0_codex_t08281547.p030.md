# p030 public contact and brand entrypoint acceptance

## Scope and safety boundary

- Validated only the local Astro site at `http://localhost:4321`.
- External Stripe and Telegram targets were inspected from rendered link attributes; no payment flow, external submission, production deployment, or remote-server action was performed.

## Source audit

- `src/site.ts` is now the single source for the confirmed Stripe URL, Telegram URL, QQ group number, and gateway-submission email.
- `src/pages/llm-gateway.astro` imports that email instead of duplicating it.
- Chinese, English, and Russian READMEs all record and display the confirmed Stripe, Telegram, QQ, and email values.
- A scan found no empty Markdown destinations in actual source/content. The only remaining `href=""` values are the intentionally blank X placeholders in the three README badges.

## Browser entry checks

- Opened `http://localhost:4321/llm-gateway`, clicked the real `提交中转站` button, and observed an open submission dialog.
- The dialog presents `发送邮件：xiu.juan2love@gmail.com` with a `mailto:` URL and a `Telegram 联系` button to `https://t.me/+AX9TXrzMaS04OWI1`.
- The same page header served `/lovemoney-mark.svg` and a `我要赞助` link to the approved Stripe URL.
- At 390px, opened the real menu on `http://localhost:4321/partnership`; it exposed `支持作者` to Stripe, Telegram to the confirmed group, and `QQ群 · 1106704568`. The viewport override was reset after the check.

## Verification

- `pnpm test`: **97 passed**.
- `pnpm typecheck`: **0 errors, 0 warnings, 0 hints**.
- `pnpm build`: passed.
- `git diff --check`: passed.
