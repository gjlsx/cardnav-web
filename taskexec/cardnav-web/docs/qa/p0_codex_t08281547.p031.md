# p031 全量需求审计与遗留直发发布入口收口

- Date: 2026-08-29
- Result: **pass:minor-refactor-done**
- Detail: `details/d_t08281547.p031.md`

## 审计基线

- 原始范围：`details/d_t08281547.overall.md`
- 现行数据合同：`details/d_t08281547.p024.md`–`p028.md` 与 `docs/collection-data-lifecycle.md`
- 公共入口收口：p029/p030 及其浏览器证据

## 结果

| 范围 | 结论 | 证据 |
|---|---|---|
| 三语公开页面、品牌、赞助/Telegram/QQ/邮箱 | 已满足 | `pnpm test` 的 97 项测试含公共入口、Sponsor、三语和 SEO 断言；p030 已完成桌面/窄屏实际浏览器检查。 |
| MySQL 替代 PostgreSQL、标准 SKU/中转/官方计划/模型排行 | 已满足 | 前端测试覆盖 schema、优先级与 `site.score` 分离、目录关系、非空排行及视频生成空态。 |
| 多来源本机采集 → 统一 raw → 显式合并入库 | 已满足并复核 | Python 53 项严格警告测试通过；`merge_import_batch` 跨来源处理优先级、同级最低价、手工闸门和事务失败回滚。 |
| 手工单条编辑/隐藏 | 保留且符合需求 | 网站配置 Tab 的显式人工操作仍使用独立覆盖与运行时快照刷新；本次未移除。 |
| 旧 staging 兼容 | 已满足 | 只保留 `collection_staging_observations` 供历史查看；运行时代码不存在第二张 staging 表。 |
| 合作运维双确认 | 已满足本机实现 | 现有 GUI 流程和 p017 验收保持；本次未扩大为远程爬虫或无人值守发布。 |
| 独立站生产发布 | **仍为外部 partial** | p008/p022 已记录；本次只读探测 `206.119.177.74:22` 返回空 SSH banner，未认证、未上传、未执行远程命令。 |

## 本次必要重构

发现 `PublicPublisher.publish_run()` 仍保留可直接从 legacy staging 写入运行时表/快照的路径，虽然当前 CLI 与 GUI 均未调用，但它会绕开“同批多来源 raw → merge/import”的去重合同。

- 已移除该入口和仅服务于它的 repository staging 发布方法。
- 保留 `PublicPublisher.publish_merged_row()` 给 `merge_import_batch` 使用，以及网站配置 Tab 的显式手工编辑路径。
- 主 overall 文档顶部已明确：p024–p028 和 `docs/collection-data-lifecycle.md` 是当前数据流权威，旧 staging 描述只作为原始需求历史。

## 未做的过设计

- 未新建 staging 表、未清库、未改数据格式、未把本机工具搬上生产。
- 未重试部署或操作服务器：SSH 在 banner 前断开，继续执行会产生假成功风险。

## 验证

```text
python -W error::ResourceWarning -m unittest discover -s scripts/collection/tests -q
Ran 53 tests ... OK

pnpm test
97 passed

pnpm typecheck
0 errors, 0 warnings, 0 hints

pnpm build
Complete

static lifecycle audit
no runtime publish_run / no collection_staging_records / no legacy staging publish helpers
```
