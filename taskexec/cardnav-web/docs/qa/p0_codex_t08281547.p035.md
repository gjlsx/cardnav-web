# p035 production release and restricted remote MySQL verification

Date: 2026-08-29  
Task: `t08281547.p035`  
Detail: `details/d_t08281547.p035.md`

## Local verification

- Reference seed succeeded: 5 sources, 9 products, 7 sites.
- `pnpm test`: 97 passed, 0 failed.
- `pnpm typecheck`: 0 errors, 0 warnings, 0 hints.
- `pnpm build`: succeeded.

## Production verification

- `ai.lovemoney.live`: Apache host-header HTTP 80 and HTTPS 443 both returned 200.
- Main public routes `/shops`, `/llm-gateway`, `/official-price`, `/model-leaderboard`, and `/guide` all returned 200.
- The public in-app browser loaded `https://ai.lovemoney.live/` with the expected AI LoveMoney title and visible brand/navigation text.
- `dtch.yg2022.top` LikeShop endpoints `/shop` on 8086, `/mobile` on 8090, and `/admin` on 8095 all returned 200.
- `apache2`, `ai-lovemoney`, and `ai-lovemoney-mysql-firewall` are active; no VPS collection timer/process was found.

## Remote MySQL containment

- Remote non-root account count for `216.144.231.55`: 1.
- Non-local root account count: 0.
- Account privileges are only CRUD on `ailovemoney.*`.
- A persistent firewall service allows loopback and the one approved external IP on TCP/3306, then drops all other TCP/3306 traffic.
- Passwords, dump content, and `.env` values are intentionally excluded from this report.
