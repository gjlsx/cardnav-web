# p036 24 小时来源新鲜度合并规则验收

## TDD 证据

先添加回归测试后，以下命令按预期失败：

```powershell
python -m unittest scripts.collection.tests.test_merge_import.RuntimeImportTests.test_fresh_lower_priority_raw_replaces_high_priority_raw_after_24_hours scripts.collection.tests.test_merge_import.RuntimeImportTests.test_all_expired_raw_candidates_leave_existing_runtime_rows_untouched
```

失败证明旧逻辑会让超过 24 小时的 PriceAI 高优先级 raw 继续覆盖新鲜的 OpenPrice raw，并会将全部过期候选写入运行时。

## 通过证据

```powershell
python -m unittest discover -s scripts/collection/tests
```

结果：`Ran 55 tests`，`OK`。

覆盖结论：

- 新鲜高优先级来源仍优先（既有单来源低优先级 batch 回归）。
- 高优先级 raw 超过 24 小时后，新鲜低优先级 raw 接管。
- 全部候选超时时，batch 标记已处理但不调用 runtime publisher 或重建快照，因此现有前端记录及最后采样时间保持不变。

## 自审

- 仅使用本机 `captured_at` 判定 24 小时窗口，未相信第三方 `observed_at`。
- 未改变 raw-first、手工闸门、事务发布、未批准来源不得联网或前端展示逻辑。
- 未触及现有未提交的 `README.en.md` 与 `README.ru.md` 编辑。
