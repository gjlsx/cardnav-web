# t08292201.p037 验证记录

日期：2026-08-29 22:04 Asia/Hong_Kong

## 范围

- 记录本机独立 merge/import worker 的单例、单线程、10 秒轮询和默认关闭约束。
- 记录 `D:\work\dock\testcatch` 的可复现实测事实，确定后续新来源优先使用 Crawlee for Python。
- 不修改采集代码、来源批准状态、MySQL 数据或生产发布配置。

## 实测

1. 从 `D:\work\dock\testcatch` 无写入执行：

   ```powershell
   python -c "import asyncio, json, benchmark; benchmark.run_curl_cffi(); asyncio.run(benchmark.run_crawlee()); benchmark.run_scrapy(); print(json.dumps(benchmark.benchmark_report, ensure_ascii=False, indent=2))"
   ```

   三套 HTTP 客户端均取得 PriceAI HTTP 200 和页面内 30 次 `sourceStoreName`；LDXP 同为 HTTP 200 但被识别为挑战页。总耗时观察为 curl_cffi 2.900 秒、Crawlee 1.889 秒、Scrapy 3.126 秒。未执行 `test_ldxp_slider.py`。

2. 无写入 RSC 试验：

   ```powershell
   python D:\work\dock\testcatch\parse_rsc.py
   ```

   输出 `Matched 29 items by escaped JSON`，证明字符串出现次数不是规范报价条数。

3. 项目回归：

   ```powershell
   python -m unittest discover -s scripts/collection/tests
   ```

   结果：`Ran 55 tests in 7.896s`，`OK`。

4. 文档检查：`git diff --check` 通过。

## 自审

- 需求：已记录 worker 的边界与 10 秒串行轮询；明确它不采集网页、不处理验证码、不在生产运行。
- 决策：未把硬编码评分当作结果；Crawlee 只作为后续、经批准来源的首选适配器，且明确尚未接入。
- 安全：不运行/不集成验证码滑块、指纹伪装或 robots 绕过代码。
- 重构：本任务为文档与验证记录，无代码重复或需重构项。
