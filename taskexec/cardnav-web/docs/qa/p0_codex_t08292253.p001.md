# t08292253.p001 QA

- 2026-08-29: `python -m unittest scripts.crawlee_collection.tests.test_sources` — 3 passed.
- 2026-08-29: `python -m unittest discover -s scripts/collection/tests` — 55 passed.
- Checked: registry contains exactly the three fixed PriceAI URLs; arbitrary source IDs and URLs are rejected; browser concurrency constant is one; the legacy collection README points to the new package without deleting old files.
