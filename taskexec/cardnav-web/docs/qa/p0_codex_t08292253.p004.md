# t08292253.p004 验证记录

日期：2026-08-29 23:13 Asia/Hong_Kong

## 修改

- `scripts/crawlee_collection/crawler.py`：仅通过 Crawlee `PlaywrightCrawler` 抓取一个 allowlist URL，`max_concurrency=1`，`max_requests_per_crawl=1`，不 enqueue 下游链接。
- `scripts/crawlee_collection/cli.py`：`check-config`、`list-sources`、`collect --source|--all`、`list-raw`、`merge-once`、`worker --once`。没有任意 URL 参数，也不导入 legacy `collect.py`。
- 采集一批次调用既有 `run_collection_batch` 只写 raw；worker 默认关闭。

## 验证

```powershell
python -m unittest scripts.crawlee_collection.tests.test_cli
python -m unittest scripts.crawlee_collection.tests.test_cli scripts.crawlee_collection.tests.test_database_worker scripts.crawlee_collection.tests.test_sources scripts.crawlee_collection.tests.test_priceai_parser
python -m unittest discover -s scripts/collection/tests
python -m scripts.crawlee_collection check-config
```

结果：CLI 5 passed；crawlee 17 passed；legacy 55 passed。`check-config` 列出三个精确 PriceAI URL 且 `max_concurrency=1`。CLI 测试使用注入的 fetcher/store，无真实网络。

## 自审

- 命令选择、allowlist、单批次 persist、merge-once、worker --once 均有测试。
- 结论：`pass:no-refactor-needed`。
