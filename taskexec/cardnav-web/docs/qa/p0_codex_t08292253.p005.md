# t08292253.p005 验证记录

日期：2026-08-29 23:38 Asia/Hong_Kong

## 修改

- Playwright 采集把 storage 放到临时目录，避免在仓库写入 Crawlee `./storage`。
- Parser 在没有 `data-collection-card` 时，从公开嵌入 JSON 提取白名单字段：卡网报价用 listing `url` 主机名（不请求该 URL），官方页提取 `type=official` 对象和 `$N/月` 套餐。

## 自动化

```powershell
python -m unittest scripts.crawlee_collection.tests.test_priceai_parser scripts.crawlee_collection.tests.test_cli scripts.crawlee_collection.tests.test_sources scripts.crawlee_collection.tests.test_database_worker
python -m unittest discover -s scripts/collection/tests
```

结果：crawlee 20 passed；legacy 55 passed。

## 本机真实采集（不记录 raw 正文或密钥）

```powershell
pnpm exec astro dev status
python -m scripts.crawlee_collection collect --all
python -m scripts.crawlee_collection merge-once --batch batch-20260829153013-239622
```

| 项 | 结果 |
|---|---|
| batch | `batch-20260829153013-239622` |
| 时间 | 2026-08-29 23:30 Asia/Hong_Kong |
| 三来源 raw payload | card 479692 chars；official 256838；transit 1277511；均无采集失败 |
| raw records | 67 valid（card 56 `shop_product`，official 11 `official_plan`，transit 0 条解析行） |
| merge-once | `runtime_writes=67`，`skipped_manual=0`，status `imported` |
| 运行库 | `shop_products` 中 `priceai-card-subscriptions` 56 行；`official_prices` 中 `priceai-official-api` 11 行；新 `gateway_sites` 0 行 |

中转 API 页有公开排行表格和赞助区块，嵌入 JSON 没有可映射的站点 homepage，因此只保留 raw payload，不伪造 host。未跟随下游链接，未处理登录或 CAPTCHA。

## 本地网站读取

Astro 曾中断，已用 `pnpm dev` 重新拉起 `http://127.0.0.1:3101/`。Playwright Chromium：

| URL | 观察 |
|---|---|
| `/shops` | 标题「卡网商品」；可见 `GPT Go`（对应已入库非 sample 行）；页面仍同时显示原参考样例 |
| `/official-price` | 标题「官方网站」；快照 `official-prices` 含 11 处 `priceai`；索引页仍以原参考样例/非实时说明为主 |
| `/llm-gateway` | 仍为 5 条参考样例中转站，与 transit 0 条可映射记录一致 |

`agent-browser` 本机 daemon 未能启动；改用已安装的 Playwright Chromium 访问同一 3101 入口。

## 自审

- 三段验收已分开：raw 审计、merge 运行库、网站读取。
- 中转页结构限制已记录，没有用字符串计数或空快照覆盖旧数据。
- 结论：`pass:no-refactor-needed`。
