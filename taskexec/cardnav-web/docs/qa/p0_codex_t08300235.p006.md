# p006 自动化验证：CardNav 中转站采集合同

## 范围

- 模型白名单只读取 `https://priceai.cc/api-transit/models`；模型规范 ID 为 trim、连续空白合并、Unicode casefold、空格替换为 `-`。
- CardNav 只以 `https://cardnav.xyz/llm-gateway` 为入口，并且只采集列表直接渲染出的同域 `/llm-gateway/<safe-slug>` 一层详情页。列表和详情的“加载更多”仅扩展当前允许页面中的渲染数据，不会访问外部“打开”链接或模型页。
- raw metadata 承载一站多模型的覆盖和公开价格；既有 merge/import 事务写 `gateway_sites`、`gateway_model_coverage`、`gateway_model_prices` 与公开快照。来源评分不覆盖展示初始值 50。

## 自动化结果

| 命令 | 结果 |
|---|---|
| `python -m unittest discover -s scripts/crawlee_collection/tests` | 34 passed |
| `python -m unittest discover -s scripts/collection/tests` | 55 passed |
| `pnpm run typecheck` | Astro typecheck: 0 errors, 0 warnings |
| `python -m scripts.crawlee_collection check-config` | 通过；配置只登记固定来源，采集/merge 默认关闭 |
| `git diff --check` | 通过（仅 Git 的 CRLF 提示，无空白错误） |

`test_cardnav_gateway.py` 覆盖模型标准化、PriceAI 白名单解析、CardNav 同域详情 URL 限制、外部链接拒绝、一站多模型/价格 raw 数据、发布后的覆盖、价格和快照，以及 metadata 在 raw 清洗时保留。

## 未在本任务执行的动作

没有触发真实网络采集、MySQL 写入或站点页面验证；这些有状态验收保留给依赖已满足的 p007。
