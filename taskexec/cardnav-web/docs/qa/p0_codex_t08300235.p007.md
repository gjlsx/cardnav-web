# p007 本机真实采集、入库与页面验收

## 时间与来源范围

- 执行时间：2026-08-30 02:51–03:05 Asia/Hong_Kong。
- 命令：`python -m scripts.crawlee_collection collect --source cardnav-gateway-details`，随后 `python -m scripts.crawlee_collection merge-once --batch batch-20260829185844-652179`。
- 仅访问 PriceAI `api-transit/models`、CardNav `llm-gateway` 列表，以及由该列表直接发现的 CardNav 同域一层详情页；未访问任何站点外链、登录、Cookie、验证码或挑战页。

## 原始采集与合并结果

| 项目 | 结果 |
|---|---:|
| PriceAI 模型目录 raw payload | 1 |
| CardNav 列表 + 详情 raw payload | 419（1 列表 + 418 详情） |
| CardNav raw records | 418，全部 valid |
| 唯一站点稳定键 | 417 |
| Crawlee 详情请求 | 418 完成，0 失败 |
| merge/import 运行时写入 | 417 |
| 白名单模型覆盖 | 2,283 |
| 公开价格记录 | 2,283 |

模型目录经规范化后得到 52 个公开标准模型 ID。仅这些 ID 的详情覆盖和价格入库；例如页面可见 `gpt-5.6-luna`，不以 CardNav 分类标签替代模型 ID。

## 运行库、快照与浏览器验收

- 只读 MySQL 核验：本次来源的 417 个 `gateway_sites` 均写入可访问性字段；`gateway_model_coverage` 2,283 条、`gateway_model_prices` 2,283 条。`gateway-sites` 快照共 422 站点，`gateway-models` 快照共 52 模型。
- 真实页面初验发现“有价格数据”为 0：原因是旧快照重建只读取覆盖，未读取 `gateway_model_prices`。以先失败测试补齐最小发布层修复（提交 `37f7411`），并使用既有 `PublicPublisher.rebuild_snapshots` 在单一本机事务刷新两个快照。
- 最终浏览器验收：`http://127.0.0.1:3101/llm-gateway` 显示 422 个站点、192 个有价格数据的站点、52 个模型。`/llm-gateway/1xm-ai` 显示 17 个模型和 17 条公开价格，包括 `gpt-5.6-luna`（输入 37.5、输出 300、单位 `$ / 1M tokens`）。

## 回归

- `python -m unittest discover -s scripts/crawlee_collection/tests`：35 passed。
- `python -m unittest discover -s scripts/collection/tests`：55 passed。
- `pnpm run typecheck`：0 errors、0 warnings。

`3101` 是已存在但未由 Astro CLI 管理的 Node 进程；为验证当前仓库还临时启动了 Astro `3102`，两处最终都读到同一数据库快照。不会停止或改动既存 `3101` 进程。
