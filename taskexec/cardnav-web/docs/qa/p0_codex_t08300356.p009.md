# t08300356.p009 实现验证

- 实现提交：`6d2c1ea`。
- `pnpm exec tsx --test test/gateway-reference-samples.test.ts test/public-shop-data-boundary.test.ts`：14/14 通过。
- `python -m unittest discover -s scripts/collection/tests -p 'test_*.py'`：57/57 通过。
- `pnpm run typecheck`：0 errors。
- `pnpm run build`：成功。
- 本机 schema 已执行幂等迁移，运行库读取确认 422 个 `gateway_sites` 均可读取 `region`、`benefit_text` 两列；随后只重建 `gateway-sites` / `gateway-models` 公开快照。
- `pnpm test` 的唯一失败是预存工作区删除项 `README.ru.md` 使 `test/public-brand.test.ts` 无法读取文件；与本任务文件未相关。真实浏览器验收留给依赖任务 p010。
