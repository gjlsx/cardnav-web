# t08300447.p010 GitHub 中转站实时参考榜合同

- 完成时间：2026-08-30 Asia/Hong_Kong
- 唯一采集入口：`https://raw.githubusercontent.com/hvoyai/awesome-ai-api/main/data.json`

## 已验证

1. 公开 raw JSON 可直接读取；当前响应含 `updatedDate`、`generatedAt` 和 `sites`，站点记录含 `rank`、`modelCount`、`uptime`、`latencyMs`、`userRating`、`ratingCount`、支付/退款/发票等字段。
2. allowlist 在 `scripts/crawlee_collection/sources.py` 固定为唯一 exact URL；README 或任意其它 URL 会由 `require_allowed_source` 拒绝。
3. 该来源登记为 `RecordKind.GATEWAY_SITE` / `source_class=reference`，其 `rank` 只可作为 `source_rank`，不可作为 AIGATE 评分、模型能力或价格排名。
4. 文档明确 Hvoy 详情 URL 不得继续采集；真实采集和 merge/import 留给 p017。

## 自动化验证

`python -m unittest scripts.crawlee_collection.tests.test_sources`：5 tests passed。

## 访问边界

`raw.githubusercontent.com/robots.txt` 没有可读取的 robots 文件（404）；本任务仅登记公开 raw JSON，不在此任务执行真实采集。p017 将在真实读取前再次按精确 allowlist 验证响应并记录结果。
