# t08300447.p011 GitHub 中转参考 raw→快照适配器

- 完成时间：2026-08-30 Asia/Hong_Kong
- 实现提交：`7eb5174`

## 已完成

1. Hvoy `data.json` 通过 Crawlee 的同一 allowlist fetcher 读取；JSON 响应用 body 文本解析，采集只写 raw payload 和 `collection_raw_records`。
2. 仅接受 `https://www.hvoyai.com/sites/<safe-slug>/` 形式的上游详情引用，转为不是真实站点 host 的 opaque raw key；缺少安全引用 URL 的记录不产生 raw 行。
3. 来源 rank、上游更新时间、模型数/厂商、在线率、延迟、用户评分、支付/退款/发票均保留在白名单 `metadata_json`。
4. 既有 merge/import 事务遇到该来源只更新 `gateway-reference-ranking` 公共快照，不创建/覆盖 `gateway_sites`，因此不会改写本站站点评分、手工地区、福利或价格数据。

## 自动化验证

`python -m unittest scripts.crawlee_collection.tests.test_hvoyai_reference scripts.crawlee_collection.tests.test_sources scripts.crawlee_collection.tests.test_cli scripts.crawlee_collection.tests.test_cardnav_gateway`：22 tests passed。
