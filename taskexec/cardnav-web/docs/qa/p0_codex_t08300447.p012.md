# t08300447.p012 GitHub 8 小时本机采集控制

- 完成时间：2026-08-30 Asia/Hong_Kong
- 实现提交：`6e7fb67`

## 已完成

1. 唯一 `catch.config`/模板登记 `hvoyai-awesome-ai-api`，其 `source_settings` 固定默认 `enabled=false`、`interval_minutes=480`；本地配置可编辑该值。
2. 配置解析会拒绝未知来源、非对象设置和小于 1 的间隔；循环 CLI 根据单个来源配置换算为 28,800 秒。
3. GUI 的“采集”页对参考来源提供“抓取一次参考 / 采集参考 / 停止参考”；所有动作都是同一固定 CLI，不接收 URL 参数。
4. 未安装 Windows Task Scheduler、cron 或服务；循环只在本机 GUI 启动的子进程存活期间运行，merge 继续是默认关闭的独立单例。

## 自动化验证

`python -m unittest scripts.crawlee_collection.tests.test_hvoyai_schedule scripts.crawlee_collection.tests.test_hvoyai_reference scripts.crawlee_collection.tests.test_sources scripts.crawlee_collection.tests.test_cli scripts.crawlee_collection.tests.test_cardnav_gateway scripts.crawlee_collection.tests.test_catch_config`：30 tests passed。
