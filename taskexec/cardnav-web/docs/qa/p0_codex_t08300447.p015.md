# t08300447.p015 价格参考页签

- 实现提交：`9729932`
- 自动化验证：`pnpm run typecheck`，0 errors / 0 warnings。

`/ranking-reference?tab=prices` 仅查询既有 `gateway_model_prices`：四种价格方向分别按 `model_id + model_family + unit` 求最低数值、覆盖站点数与最新采集时间。SQL 过滤空单位和空数值，GitHub 参考快照、倍率和描述文本不进入该查询；页面对无可比较数据给出空态和数据来源说明。
