# t08300447.p018 中转站详情社群入口验收

- 完成时间：2026-08-30 Asia/Hong_Kong
- 实现提交：`68c7798`

## 行为

详情福利卡保留站点外部“领完福利前往”，并新增两个不展示地址的独立社群按钮：`TELG` 和 `QQ群`。两者都使用 `target="_blank"` 与 `rel="noopener noreferrer"`。

## 自动化

- 先新增回归测试，确认旧实现缺少 `TELG` 和 `QQ群` 后失败；最小实现后 `test/gateway-reference-samples.test.ts` 8 passed。
- `pnpm test`：103 passed。
- `pnpm run typecheck`：0 errors / 0 warnings。
- `pnpm run build`：通过。

## 3101 浏览器验收

从 `http://127.0.0.1:3101/llm-gateway` 的实际详情入口进入 `http://127.0.0.1:3101/llm-gateway/fishxcode-com`：

- 两个按钮均可见，标签为 `TELG`、`QQ群`。
- 两个目标均为新窗口链接；页面可见文本不含 Telegram 或 QQ 的 URL。
- 控制台 error 为空。
- 截图：`p0_codex_t08300447.p018.gateway-detail-3101.jpg`。
