# t08300447.p019 VPS 正式发布与浏览器验收

- 完成时间：2026-08-30 Asia/Hong_Kong
- 发布方式：严格执行 `howtorunvpsnew.md` 的 `python scripts/deploy/publish_ai_lovemoney.py`。

## 发布前

- `pnpm test`：104 passed。
- `pnpm run typecheck`：0 errors / 0 warnings。
- `pnpm run build`：通过。
- 没有执行 `seed:reference-samples`，以保留本机已验证的真实采集与 merge/import 运行时数据。

## VPS 受限发布结果

- 已备份独立站和服务器本地 `ailovemoney`，仅同步 `dist` 与本机数据库导出；远端 `.env` 被保留。
- 只重启 `ai-lovemoney`；Apache 和独立 Node 服务均为 `active`，Node 只监听 `127.0.0.1:3101`。
- 导入后计数：公开快照 11、中转站 422、逐模型价格 2283。
- origin HTTP 80 / HTTPS 443 均为 200；`/`、`/shops`、`/llm-gateway`、`/official-price`、`/model-leaderboard`、`/guide` 均为 200。
- LikeShop `8086/shop`、`8090/mobile`、`8095/admin` 均为 200；未发现生产采集进程或 collect/crawlee timer。

## 正式浏览器验收

在 `https://ai.lovemoney.live/` 实际打开首页、商品、中转站、官方价格、模型排行、指南、排名参考；页面标题均显示 AIGATE。

从生产中转站列表进入 `fishxcode` 详情：

- 桌面和 390px 移动端均显示 `TELG` 与 `QQ群`；均为新窗口目标，正文不显示 Telegram 或 QQ URL。
- 浏览器控制台 error 为空。
- 截图：`p0_codex_t08300447.p019.production-gateway-detail.jpg`、`p0_codex_t08300447.p019.production-gateway-detail-mobile.jpg`。

## 边界

- 按用户最新决定，旧域暂不重定向到尚未接入的 `aigate.live`；AIGATE 可见品牌已发布。新域接入后需另开任务恢复重定向。
