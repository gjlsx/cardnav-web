# t08300447.p020 暂停旧域重定向验收

- 完成时间：2026-08-30 Asia/Hong_Kong
- 实现提交：`0b86d3d`

## 结果

- 已仅移除 `ai.lovemoney.live -> aigate.live` 的 308 逻辑；AIGATE 可见品牌、站点常量和其他路由逻辑不变。
- 新增回归测试先在旧重定向实现上失败，移除后通过。
- `pnpm exec tsx --test test/public-brand.test.ts`：7 passed；`pnpm run typecheck` 和 `pnpm run build` 均通过。
- 用新构建启动临时的本机 Astro standalone 3102，`--resolve ai.lovemoney.live:3102:127.0.0.1` 请求返回 HTTP 200，而非重定向；临时进程已停止。

后续待用户接入新域后，再单独恢复旧域跳转任务。
