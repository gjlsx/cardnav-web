# t08300447.p021 QA：恢复旧域 AIGATE 308 跳转

- 日期：2026-08-31（Asia/Hong_Kong）
- 实现提交：`2a1c91c codex_t08300447.p021: restore AIGATE redirect`

## 验证结果

1. 先执行定向回归测试，旧中间件未包含旧域跳转，测试按预期失败；恢复后执行 `pnpm exec tsx --test test/public-brand.test.ts`，7/7 通过。
2. `pnpm run typecheck` 通过：0 errors、0 warnings、0 hints；`pnpm run build` 通过。
3. 以构建后的独立 Node 实例（`PORT=3102 node dist/server/entry.mjs`）请求 `http://ai.lovemoney.live:3102/llm-gateway?model=gpt`，收到 `HTTP/1.1 308 Permanent Redirect`，Location 为 `https://aigate.live:3102/llm-gateway?model=gpt`。临时端口仅为本机验收用途；路径和查询参数均完整保留。
4. 使用真实浏览器打开 `https://aigate.live/`，最终地址为该新域，页面标题为 `AIGATE - 一站式 AI 大模型账号购买导航`，且首页正常渲染。截图：`p0_codex_t08300447.p021.aigate-browser.jpg`。

生产旧域的真实浏览器重定向将随 p022 发布后再次验收。
