# t08300447.p022 QA：AIGATE 跳转正式发布与验收

- 日期：2026-08-31（Asia/Hong_Kong）
- 发布方式：`python scripts/deploy/publish_ai_lovemoney.py`

## 发布前验证

- `pnpm test`：104/104 通过。
- `pnpm run typecheck`：0 errors、0 warnings、0 hints。
- `pnpm run build`：通过。
- 未运行会重置真实运行快照的参考样例 seed。

## VPS 发布与服务边界

- 独立站构建、数据库快照导入和 `ai-lovemoney` 重启均成功；`apache2` 与 `ai-lovemoney` 均为 active。
- 导入后校验：`public_snapshot_entries=11`、`gateway_sites=422`、`gateway_model_prices=2283`。
- 源站 `http:80` 与 `https:443` 的旧域请求均返回 308，符合旧域迁移预期。
- LikeShop 保持可用：`/shop/` 8086、`/mobile/` 8090、`/admin/` 8095 均返回 200。
- 生产环境无 collection/crawlee 进程，且无 collect/crawlee systemd timer；发布没有启用采集或调度。

## 真实浏览器验收

在真实浏览器打开：

`https://ai.lovemoney.live/llm-gateway?model=gpt#list`

浏览器最终地址为：

`https://aigate.live/llm-gateway?model=gpt#list`

路径、查询参数与片段均保留；页面标题为 `AI 大模型 API 中转站 - AIGATE`，中转站页面正常渲染，浏览器 console error 为 0。截图：`p0_codex_t08300447.p022.old-domain-redirect.jpg`。
