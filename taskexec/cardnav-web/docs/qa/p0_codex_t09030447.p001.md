# t09030447.p001 QA：全局右下回到顶部按钮

- 日期：2026-09-03（Asia/Hong_Kong）

## 自动验证

1. 新增 `test/back-to-top.test.ts` 后先执行目标测试，因按钮和脚本尚不存在而按预期失败。
2. 实现后执行 `pnpm exec tsx --test test/back-to-top.test.ts`：2/2 通过。
3. `pnpm run typecheck`：0 errors、0 warnings、0 hints。

## 本机真实浏览器验收

- 运行态：当前仓库 Astro dev，`http://127.0.0.1:3101/`。
- 页面：`http://127.0.0.1:3101/llm-gateway`。
- 初始：`scrollY=0`，回顶按钮为 hidden。
- 向下滚动后：`scrollY=2711.199951171875`，按钮显示；截图为 `p0_codex_t09030447.p001.back-to-top-visible.jpg`。
- 点击 `data-back-to-top` 后：`scrollY=0`，按钮恢复 hidden。
- 浏览器 console error：0。

## 自检

- 需求：共享公开页右下固定按钮、下滚一屏显示、平滑回顶均满足。
- 范围：仅修改共享布局、共享前端脚本和对应测试；Guide 独立滚动页面未加入按钮。
- 无重复逻辑或需进一步重构项。
