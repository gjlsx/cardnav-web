# t09030447.p002 QA：允许来源真实采集并入本地库

- 日期：2026-09-03（Asia/Hong_Kong）
- 采集仅在本机执行；没有在 VPS 运行采集器或定时任务。

## 配置与运行环境

- `python -m scripts.crawlee_collection check-config` 通过：5 个配置来源、固定 allowlist、并发 1；采集和 merge 循环均为 disabled。
- 首次真实采集确认 Python Playwright 缺少其对应 Chromium/headless shell，因此失败；未执行 merge/import，既有运行库未被改写。
- 已运行官方 `python -m playwright install chromium` 补齐本机依赖缓存后重试；该安装未写入仓库，也未触及生产服务器。

## 真实 raw -> merge/import

- `python -m scripts.crawlee_collection collect --all` 成功，所有来源 failures 为空。
- raw batch：`batch-20260902210546-248512`。
- raw records：1269；CardNav 同域一层详情页完整读取 422/422，0 failed requests。
- `python -m scripts.crawlee_collection merge-once --batch batch-20260902210546-248512` 成功：`runtime_writes=1268`、`skipped_manual=0`、`already_imported=false`。
- 未记录或提交第三方 raw 页面正文、账号、Cookie、Token、密码或数据库连接信息。

## 本机读取与回归

- `python -m unittest discover -s scripts/crawlee_collection/tests`：42 tests 通过。
- 真实浏览器打开 `http://127.0.0.1:3101/llm-gateway` 并刷新：页面读取 426 个中转站、197 条价格数据、51 个模型记录，首行最近刷新为 `2026-09-02 20:57:53`。
- 截图：`p0_codex_t09030447.p002.local-gateway.jpg`。

## 自检

- 采集与 merge/import 为两个明确命令；没有新增 URL、外部商户跳转、绕过访问控制或自动调度。
- 失败批次未 merge；成功批次才写入本机运行库和公开快照。
