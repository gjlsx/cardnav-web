# t09030447.p003 QA：独立 VPS 发布与正式浏览器验收

- 日期：2026-09-03（Asia/Hong_Kong）
- 发布遵循 `howtorunvpsnew.md`，仅使用 `python scripts/deploy/publish_ai_lovemoney.py`；未上传远端 `.env`，未运行会覆盖真实数据的 sample seed，未在生产运行采集器或定时任务。

## 发布前校验

- `pnpm test`：106/106 通过。
- `pnpm run typecheck`：通过，0 errors / warnings / hints。
- `pnpm run build`：通过。

## 发布结果

- 发布脚本成功上传独立站构建和当前本机运行库；远端库汇总为 11 个公开快照、426 个中转站、2344 条中转站模型价格记录。
- `ai-lovemoney` 与 `apache2` 服务均为 active；生产环境没有 collector 进程或 collection timer。
- 旧域源站 HTTP/HTTPS 均返回 308；LikeShop 的 8086、8090、8095 三个端口均返回 200。
- 发布过程未改动 LikeShop 配置，也没有将数据库凭据、连接串、Cookie、Token 或第三方 raw 正文写入任务证据。

## 正式浏览器验收

- 真实浏览器从 `https://ai.lovemoney.live/llm-gateway` 打开，最终地址为 `https://aigate.live/llm-gateway`，标题为“AI 大模型 API 中转站 - AIGATE”。
- 页面显示“已收录中转站 426、有价格数据 197、模型记录 51”；列表首行最近刷新时间为 `2026-09-02 20:57:53`。
- 下滚长页后，右下固定“回到顶部”按钮可见；点击后回到页面顶部，按钮按阈值隐藏。浏览器控制台未发现 error。
- 真实浏览器滚动截图已在本次验收中可见；仓库中旧的空白临时截图不作为验收证据，因此没有提交。

## 自检

- 发布仍保持 raw -> merge/import -> runtime/public snapshot 的既有数据链路，没有新增线上采集或写库快路径。
- 独立 AIGATE 站与 LikeShop 共同部署状态均已回归验证。
