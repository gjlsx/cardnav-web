# p004 QA — local browser validation

- Date: 2026-08-28
- Runtime state: cardnav-web Astro development service at `http://localhost:3101/`, configured for local MySQL.

## Actual browser entries

| URL | HTTP | Observed title | Result |
|---|---:|---|---|
| `http://localhost:3101/` | 200 | `AI LoveMoney - 一站式 AI 大模型账号购买导航` | no old visible domain/brand or Hero |
| `http://localhost:3101/en/about/` | 200 | `About AI LoveMoney - AI LoveMoney` | no old visible domain/brand |
| `http://localhost:3101/ru/about/` | 200 | `О AI LoveMoney - AI LoveMoney` | no old visible domain/brand |

## DOM evidence

- Excluding Astro's developer toolbar, the page had 0 GitHub URLs and 0 Telegram URLs.
- Sponsor area: 0 anchors and 4 images; no card text/price/CTA content was observed.
- An initial probe of port 5173 was rejected from evidence because it was a different project (`鼎拓示范诊所`).
- The first cardnav-web run returned homepage 500 because its prior process had no MySQL password loaded. After updating the uncommitted local runtime `.env` and restarting only the cardnav-web 3101 service, the same route returned 200.

## Supporting checks

- `pnpm run typecheck`: passed.
- `pnpm run build`: passed.
