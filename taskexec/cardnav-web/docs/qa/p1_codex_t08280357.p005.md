# p005 QA — VPS independent publish

- Date: 2026-08-28
- Executor: grok (user-authorized takeover of unfinished Codex claim)
- Target: `206.119.177.74`
- Public vhost: `ServerName ai.lovemoney.live` on Apache `:80`
- App: systemd `ai-lovemoney.service` → `127.0.0.1:3101`
- Web root: `/www/wwwroot/ai.lovemoney.live`
- Secrets: not recorded

## Diagnosis

- OS: Ubuntu 24.04.1 LTS, disk 39G free
- Apache already listened on `80/8086/8090/8095`
- Port 80 occupant is LikeShop `dtch.yg2022.top`, not an unrelated site, so it was kept
- Node existed only as `/usr/local/bin/node` → `/root/.hermes/node/bin/node`; `/root` is `700`
- MariaDB active; no previous `ai.lovemoney.live` directory
- Server DNS lookup for `ai.lovemoney.live` returned Cloudflare IPv6, not this VPS. Verification used `Host: ai.lovemoney.live` against `206.119.177.74`

## Deploy choices

- Added a name-based Apache vhost; did not edit the LikeShop `8086/8090/8095` blocks
- Copied Node to `/usr/bin/node` so `www-data` can execute it without opening `/root`
- Imported local MySQL dump into server `ailovemoney`
- `public_snapshot_entries` count after import: 9

## HTTP evidence

Server loopback:

| Check | Result |
|---|---|
| `http://127.0.0.1:3101/` | 200, HTML contains `AI LoveMoney`, no `cardnav.xyz` / 卡网大全 |
| `Host: ai.lovemoney.live` → `http://127.0.0.1/` | 200, same title check |
| `Host: dtch.yg2022.top` `:8086/shop/` | 200 |
| `Host: dtch.yg2022.top` `:8090/mobile/` | 200 |
| `Host: dtch.yg2022.top` `:8095/admin/` | 200 |
| `Host: dtch.yg2022.top` `:80/` | 200 |

From Windows to `206.119.177.74`:

| Check | Result |
|---|---|
| `Host: ai.lovemoney.live` `http://206.119.177.74/` | 200, title AI LoveMoney, no old public brand |
| `Host: dtch.yg2022.top` `:8086/shop/` | 200 |
| `Host: dtch.yg2022.top` `:8090/mobile/` | 200 |
| `Host: dtch.yg2022.top` `:8095/admin/` | 200 |
| `Host: dtch.yg2022.top` `:80/` | 200 |

`systemctl is-active ai-lovemoney` = active; `is-enabled` = enabled; Node listens on `127.0.0.1:3101` only.

## Gaps

- Public DNS for `ai.lovemoney.live` is not this VPS yet. Direct `http://ai.lovemoney.live/` was not used as the pass criterion; host-header against `206.119.177.74` was.
- Full three-language browser click-through is p006.
