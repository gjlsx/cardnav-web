# p001 MySQL store cleanup verification

- Time: 2026-08-28 Asia/Hong_Kong
- Scope: `details/d_t08280627.p001.md`

## Automated verification

- `pnpm test`: 64 passed, 0 failed.
- `pnpm run typecheck`: 0 errors, 0 warnings, 0 hints.
- `pnpm run build`: passed.
- Static migration guard confirms all nine `public_snapshot_entries` keys remain on their public read paths and `src/store.ts` contains no PostgreSQL-only query syntax or `@ts-nocheck`.
- Local MySQL smoke query completed through the store: products 0, gateway sites 0, gateway models 0, popular terms 10, official catalog 0, leaderboard task slugs 0. The zero list values match the imported snapshot payloads, not a runtime connection error.

## Browser verification

- Runtime: local cardnav-web Astro server at `http://localhost:3101`.
- Entry flow: homepage, then `/llm-gateway` and `/shops` in a real browser; all loaded successfully.
- Downstream checks: `/api/shop-products.json`, `/api/llm-gateway/sites.json`, and `/api/llm-gateway/models.json` each returned HTTP 200 with JSON content.
- Observed: QQ placeholder is present; no application Telegram/GitHub/X outbound link; no `cardnav.xyz` or `卡网大全` text on tested pages. The Astro development toolbar has two framework feedback GitHub links, excluded because it is not website content and is absent from the production build.
- Screenshot: `%TEMP%/cardnav-p001-home.png` (local, not committed).

## Self-review

Requirement satisfied; MySQL fallback behavior is preserved, migrated snapshots are prioritized, PostgreSQL dead branches and type-check suppression are removed, tests cover regression markers, names are consistent, and no required p001 refactor remains.
