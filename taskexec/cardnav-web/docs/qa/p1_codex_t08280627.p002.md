# p002 Regression and live deployment verification

- Time: 2026-08-28 Asia/Hong_Kong
- Scope: `details/d_t08280627.p002.md`
- Deployed application revision: `ee6d041` (build output only; server `.env` and MySQL data were preserved).

## Server-side verification

- `ai-lovemoney.service`: active and enabled; Node remains bound to `127.0.0.1:3101`.
- `ailovemoney.public_snapshot_entries`: 9 rows, queried read-only using the existing service environment.
- Apache host-header vhost on port 80: HTTP 200 for `ai.lovemoney.live`.
- LikeShop isolation: localhost host-header checks returned HTTP 200 on `8086`, `8090`, and `8095`; none of those services or vhosts were changed.
- Deployed `dist/server` has no `@ts-nocheck`, `ARRAY_AGG`, or `jsonb_` legacy markers.
- A prior `dist` copy was preserved under `/www/wwwroot/ai.lovemoney.live-backups/` before the switch. Server `.env` and MySQL contents were not overwritten.

## Live browser verification

- Browser entry: `https://ai.lovemoney.live/` in Chromium.
- Desktop routes: `/`, `/llm-gateway/`, and `/shops/` each returned HTTP 200. Titles were `AI LoveMoney - 一站式 AI 大模型账号购买导航`, `AI 大模型 API 中转站 - AI LoveMoney`, and `卡网商品 - AI LoveMoney`.
- Mobile homepage: HTTP 200 at 375×667.
- Downstream APIs: `/api/shop-products.json`, `/api/llm-gateway/sites.json`, and `/api/llm-gateway/models.json` each returned HTTP 200 with `application/json`.
- No tested page exposed `cardnav.xyz`, `卡网大全`, or a Telegram/GitHub/X outbound anchor.
- Screenshot: `%TEMP%/cardnav-p002-live-mobile.png` (local, not committed).

## Self-review

Required real entry, data-count, API, browser, and LikeShop isolation evidence are present. The migrated product/gateway/model snapshots contain empty lists from the source data, so empty public lists are expected and were not represented as missing migration data. No required follow-up refactor remains.
