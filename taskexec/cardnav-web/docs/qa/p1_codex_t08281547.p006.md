# p006 QA — Python 本机采集 CLI/GUI

- Executor: grok
- Date: 2026-08-28

## Commands

```text
python -m unittest tests.test_collection -v   # 9 passed, including GUI validate smoke
python collect.py check-config                # 5 sources, no errors
python collect.py dry-run                     # network_requests=0, merged=3
python collect.py write-staging               # staging_count=3, published=false
```

## Coverage

- Defaults: enabled=false, interval_minutes=60, max_items_per_run=1000, 0=unlimited
- Source config rejects `score` / `site_score`
- Unapproved sources skip HTTP and use fixtures
- Cap honored; same-priority lowest price; site_api beats aggregator
- Whitelist strips html/cookie
- GUI constructs and validates without starting a scheduler
- MySQL staging table `collection_staging_observations` anonymous count=3
- No Windows task, cron, or publish path

MySQL credentials came from repo `.env` in-process only.
