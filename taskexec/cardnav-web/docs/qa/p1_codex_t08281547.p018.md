# p018 QA — in-process scheduler

LocalScheduler skips enabled=false, marks next due after run, stops with the GUI process. No Windows Task Scheduler. Tests: SchedulerTests in `tests.test_operations`.
