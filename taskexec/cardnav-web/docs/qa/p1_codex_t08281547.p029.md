# p029 sponsor, community, and logo acceptance

## Runtime boundary

- Validated the local Astro site at `http://localhost:4321/partnership` only.
- The Stripe destination was inspected as an external link only. No Stripe checkout, payment, production deployment, or remote server operation was performed.

## Desktop entry verification

- The header uses the original local SVG asset `/lovemoney-mark.svg` for the site mark.
- The desktop header includes `我要赞助`; its `href` is `https://buy.stripe.com/cNi8wRgiq26I1Ese0V0Fi00` and it opens in a new tab.
- The partnership page includes the `支持 AI LoveMoney` card and its `前往赞助` button has the same Stripe destination and new-tab behavior.
- The header Telegram button and partnership page links use `https://t.me/+AX9TXrzMaS04OWI1`. The QQ control carries and copies the visible group number `1106704568`.

## Narrow-screen entry verification

- At a 390px viewport, opened the real hamburger menu through its accessible summary control.
- The menu contained `支持作者` to the Stripe destination, Telegram to the confirmed group destination, and `QQ群 · 1106704568` as the copy control.
- The temporary viewport override was reset after verification.

## Documentation and content consistency

- `README.md` records the Stripe URL, Telegram URL, QQ group number, display locations, and the `src/site.ts` single-source rule.
- Actual partnership and guide Telegram links in Chinese, English, and Russian no longer use empty destinations.

## Verification

- `pnpm test`: passed (95 tests).
- `pnpm typecheck`: passed (0 errors, 0 warnings, 0 hints).
- `pnpm build`: passed.
- `git diff --check`: passed.
