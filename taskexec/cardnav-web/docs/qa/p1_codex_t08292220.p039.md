# t08292220.p039 验证记录

日期：2026-08-29 22:21 Asia/Hong_Kong

## 对比范围

- `D:\phpStudy\WWW\likeshop\AGENTS.md`
- `D:\work\aiwork\lovemoney\AGENTS.md`
- 本仓库原有 `.agent-rules.md`、`tasklist_rules.md` 与采集生命周期文档。

## 结果

- 根目录新增 `AGENTS.md`，将 taskexec 细节继续路由至既有 `.agent-rules.md` / `tasklist_rules.md`，没有重复或改写任务表规范。
- 借鉴了 Likeshop 的“权威架构入口、文档分工、本地验证”和 LoveMoney 的“最终架构对齐、CLI、浏览器验证、避免旁路”。
- 未复制其他仓库专有的 PHP/UniApp 端口、Dev Bus、Redis、PyQt、`doc/desktop/` 或 CLI catalog 规则；`rg` 对这些特征无匹配。
- 新规则覆盖 CardNav 已确认的 raw-first、批准来源、Crawlee + Playwright、独立 merge/import worker、MySQL 批次连接及敏感数据边界。
- `t08292213.p038` 已增加对本任务的依赖，防止其在根规则前启动。

## 验证

```powershell
git diff --check
rg -n "taskexec|raw-first|Crawlee|Playwright|one-line CLI|MySQL|ai\\.lovemoney\\.live" AGENTS.md
rg -n "D:\\phpStudy|D:\\work\\aiwork|Dev Bus|Redis|8086|8090|8095|doc/desktop" AGENTS.md
```

结果：格式检查通过；第一条命令确认项目规则存在；第二条无输出，确认未混入参考仓库的专有规则。

## 自审

- 需求已满足：完成两份 AGENTS 的实际比较并新建本项目根规则。
- 规则与既有 `.agent-rules.md` 分工明确，没有创建多余 handoff 文档。
- 仅文档变更，不涉及浏览器、数据库或生产运行；无需额外运行应用测试。
- 结论：`pass:no-refactor-needed`。
