# t08292227.p040 验证记录

日期：2026-08-29 22:28 Asia/Hong_Kong

## 事实核对

- `README.md` 与 `docs/data-nav-and-collection.md` 记录本地受控站点为 `127.0.0.1:3101`。
- `README.md`、`docs/mysql-migration.md` 和 Python repository 默认值均记录 MySQL `127.0.0.1:3306`，且实际配置读取 `.env` `MYSQL_*`。
- `scripts/collection/README.md` 的本机控制面为 `python scripts/collection/gui.py`。
- `docs/CURRENT_STATUS.md`、`nocodb/` 初始均不存在；用户已明确批准创建前者，后者仍不存在。

## 修改结果

- 增加 q: 只问答、全局提醒路由、简短计划、极简、绝对路径内部证据规则。
- 明确 `3101`、本机 GUI、MySQL 默认端口、Astro 只读边界和 taskexec 状态交接。
- 新增 `docs/CURRENT_STATUS.md`：记录当前采集架构、p038 的待执行状态、已核验入口、最新回归、用户未提交 README 改动、约束和 `Next TODO`。
- 增加 Astro 的 typecheck/test/build 与实际浏览器入口验证规则。
- 保留平台要求的最终可点击绝对文件链接；没有写入“不允许 Markdown 文件链接”的冲突规则。
- `t08292213.p038` 增加对此任务的依赖。

## 验证

```powershell
git diff --check
rg -n "^(scope|updated_at|timezone|tags|summary):|^## Next TODO|t08292213\\.p038" docs/CURRENT_STATUS.md
rg -n "q:|3101|NocoDB|3306|CURRENT_STATUS|pnpm run (typecheck|build)|one-line CLI|absolute Windows" AGENTS.md
rg -n "4321|8080|Dev Bus|Redis|doc/desktop|forbid.*Markdown|禁止 Markdown" AGENTS.md
```

结果：格式检查通过；状态文件含必需 frontmatter 和 `Next TODO`，所需规则均存在。排除项仅匹配到“不要假定为 4321”的否定说明，未将 4321 写为当前入口；其余排除项无匹配。

## 自审

- 已采用推荐中适合 CardNav 的通用与前端规则。
- 已排除不存在的 NocoDB 和错误本地端口；状态文档在用户明确授权后初始化。
- 仅文档变更，无应用、浏览器、数据库或生产行为变更。
- 结论：`pass:no-refactor-needed`。
