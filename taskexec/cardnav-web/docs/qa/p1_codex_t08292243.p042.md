# t08292243.p042 验证记录

日期：2026-08-29 22:43 Asia/Hong_Kong

## 修改

根 `AGENTS.md` 新增 `Tasklist execution completion`：当用户明确要求执行 tasklist 时，连续执行全部依赖满足的本人任务；只允许角色/依赖不满足或有实质原因并记录为 `blocked`、`partial`、`pending` 的任务停下；最终完成前必须重读 tasklist 和自己的锁。

规则同时明确：不得把普通困难或测试需求当阻塞；不得静默漏掉 `todo`/`doing`；仅创建、审阅或检查 tasklist 的用户请求不触发全量执行，也不扩大批准边界。

## 验证

```powershell
git diff --check
rg -n -C 1 "Tasklist execution completion|explicitly asks to execute|Before claiming|runnable task|does not require executing" AGENTS.md
```

结果：格式检查通过；连续执行、允许停止条件、完成前复查与不扩大授权的规则均存在。

## 自审

- 用户规则已逐项表达，未把 `partial`/`blocked` 作为无理由中断出口。
- p038 已依赖此规则，且本任务只修改文档。
- 结论：`pass:no-refactor-needed`。
