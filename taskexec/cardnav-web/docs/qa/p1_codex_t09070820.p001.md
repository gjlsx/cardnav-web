# t09070820.p001 文档收尾 QA

- 日期：2026-09-07，Asia/Hong_Kong。
- 范围：文档静态核对；没有采集、SQL/seed、网络探测、安装或部署，没有修改业务代码。

## 核对依据与修正

| 核对源码/证据 | 文档修正 |
|---|---|
| src/i18n/config.ts、src/layouts/PublicPage.astro | zh/en/ru 及三个可用语言入口；更正前轮 README/状态的“不支持俄语”表述，经营优先级与实现能力分开 |
| scripts/crawlee_collection/sources.py、catch.config.example | 六个 registry 项与模板五个任务项，CardNav 隐式读取标准模型目录；补 Hvoy JSON 语义与 allowlist |
| cli.py、worker.py、catch_config.py | worker 已接入，模板关闭自动调度但显式 CLI 仍执行；10 秒为轮询默认值，不是硬编码不可配置参数；类内单例不等于跨进程互斥 |
| scripts/collection/collection_lib/migrations.py、pipeline.py、repository.py | 分开 raw SQL 列和 JSON payload 字段；captured_at 才是本机采样时钟，不新增第二 schema |
| scripts/deploy/publish_ai_lovemoney.py、九月三日发布 QA | 旧域 308、新域实际浏览器检查；保留真实服务/路径名称，指南不含日常 seed 步骤，脚本探测不冒充完整验收 |
| tasklist08280357、08281547、09030447 归档记录 | 标明历史设计/计划，不重跑旧 checkbox，也不把历史 partial 改成完成 |

## 静态验收

- 对本轮 19 份关联文档及两份确定版进行本地 Markdown 链接/锚点扫描：147 个本地引用、1 个标题锚点。首次扫描仅发现本 QA 尚未落盘，补齐后复验 errors=[]。格式检查发现三处新增行尾空格，已清理后复验。
- 唯一主文档仍为 PRODUCT_STRATEGY.md / ROADMAP.md；两份被合并的旧战略/路线图不存在。历史技术说明不是第三份现行路线图。
- 未增加报价、评分权重或经营承诺。既有参数按源码命名，来源与运行授权不由战略自动扩张。
- 新 tasklist 一行、18 列，任务/日志/Detail/Report 路径符合范围；其他角色的旧 todo 行未改变。
- `git diff --check` 和暂存范围检查作为提交门禁；采集引擎仅暂存独立历史提示，原有用户段落留在工作区。

## 自检与剩余边界

需求覆盖、命名/入口一致、历史/现行分层、验证记录均完成；无需业务重构。未运行产品测试或浏览器，本轮不能证明当前线上可用或翻译质量；九月三日数据仍明确为历史证据。

运行层跨进程 worker 互斥未在本轮实施，文档明确只能串行启动；新 GUI 和数据库笔记仍未跟踪。已有用户修改保留。后续按 M0/M1 推进，商业报价、评分方法、我方域名在对应实施前确定，不阻塞本次文档交付。
