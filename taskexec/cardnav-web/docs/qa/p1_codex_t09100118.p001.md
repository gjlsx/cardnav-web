# p001 外部榜单与竞品证据审计

日期：2026-09-10，Asia/Hong_Kong。环境：本机 `ailovemoney` 只读聚合查询；没有运行采集、seed、merge/import 或生产操作。

## 结论

当前模型表现数据仅是 **12 条 CardNav 公开页面历史样例**，不是已获持续引用/采集许可的真实外部榜单接入。公开展示应继续显著标为样例；若无新的可核验使用依据，来源策略为 `sample`（样例）或 `link-only`（仅安全原榜链接），不能升级为 `verified-reference`，更不能称为本站实测或本站综合评分。

## 运行数据与快照核验

对 `model_leaderboards` 按 `source_id × task_slug × source_board_slug × is_sample` 只读聚合：

| sourceId | task / board | isSample | rows | missing score | true zero score |
|---|---|---:|---:|---:|---:|
| reference-cardnav-leaderboard | coding / coding | true | 3 | 0 | 0 |
| reference-cardnav-leaderboard | creative-writing / creative-writing | true | 3 | 0 | 0 |
| reference-cardnav-leaderboard | math / math | true | 3 | 0 | 0 |
| reference-cardnav-leaderboard | text-to-image / text-to-image | true | 3 | 0 | 0 |

`public_snapshot_entries['model-leaderboards']` 为 ARRAY、12 行；按同一四元组与运行表完全一致，12 个分数均为有限数。运行表的样例 `sampled_at` 是 2026-08-27（香港时区显示），快照写入为 2026-08-28；两者均不是原榜当前更新时间。

## 证据清单

| sourceId | 原始发布方 / 转载方 | URL / 任务 | 观测 | 当前分类 | 方法和分数单位 | 使用/许可依据 | 阻碍与下一步 |
|---|---|---|---|---|---|---|---|
| reference-cardnav-leaderboard | 原始评测发布方：unknown；转载/样例名称：CardNav 公开模型排行参考 | `https://cardnav.xyz/model-leaderboard/{coding,creative-writing,math,text-to-image}` | 代码样例 2026-08-27；本轮网络打开超时或被安全限制 | sample | unknown；分数仅是原页面历史显示值 | 未找到可复核的再分发/持续使用依据 | p002 将此 source 强制为 sample；后续若要接入，先由负责人确认原发布方、精确页面、方法、版本、许可和刷新范围 |
| Hvoy AI 中转参考 | 原始/公开来源：hvoyai/awesome-ai-api；本站仅保存中转站参考 | `https://raw.githubusercontent.com/hvoyai/awesome-ai-api/main/data.json` | 当前允许来源合同 | unknown for model ability | 包含中转 rank/可用率/延迟等参考，不是模型能力分数 | 现有数据生命周期明确禁止将它变成模型能力榜 | 不进入本任务的模型表现榜；保持独立中转参考 |
| PriceAI 标准模型目录 | PriceAI | `https://priceai.cc/api-transit/models` | 当前允许 raw 目录合同 | unknown for model ability | 模型标准名目录，不含已批准能力榜语义 | 现有合同只用于本批允许模型集合 | 不把目录变成模型排名；新榜单需独立来源批准 |

## 源码风险与对标边界

| 项目 | 已具备 | 本单后续修复 | 后续范围 |
|---|---|---|---|
| 评分语义 | `sourceName`、`sourceUrl`、`sourceId`、`isSample` 已持久化 | p002 把 unknown 与真实 0 分分开、将来源策略变成显式合同 | 获批外部来源和本站测评方法 |
| 榜单展示 | 任务页签、样例徽章、站内网关/商品关系入口 | p003 统一首屏/动态加载的来源、日期、限制与安全原榜链接 | 多来源接入与可复现自测结果 |
| 竞品能力 | 历史审计记录 PriceAI/CardNav/OpenPrice 的标准产品/模型聚合、比较、渠道信息层级 | 不复制页面/素材，不把老观察当实时内容 | 在有许可和独有价值时扩展页面族 |
| 测量 | 既有 Umami/商品热度是局部起点 | p004–p006 建最小匿名路径观测 | 合格比较判据、真实基线和周去重用户 |

源码明确风险：`normalizeModelLeaderboardRow` 将 `Number(row.score) || 0` 写成默认零；首屏直接 `toFixed(2)`，而动态 API/JS 对来源字段的使用不一致；当前 Crawlee registry 无模型能力榜来源。没有证据表明 Hvoy 的服务商参考、PriceAI 目录或 CardNav 样例可作为本站模型能力实测。

## 复现与限制

- 只读 Node + mysql2 聚合查询运行表与快照；不输出连接信息或原始第三方正文。
- 源码检查：`rg -n "Number\\(row\\.score\\)|sourceName|sourceUrl|model_leaderboards" src/store.ts src/components/ModelLeaderboardContent.astro src/pages/api/model-leaderboard/[taskSlug].json.ts src/scripts/page-widgets.js src/reference-samples.ts scripts/crawlee_collection/sources.py`。
- 本轮对 CardNav 首页/任务页做有限公开读取尝试，结果为安全策略拒绝或 400 超时；这不是来源可用性或许可的正面证据。没有重试、爬取、跟随商户链接或访问群链接。

## 自审

需求：完成数据/来源审计；通过。验证：运行表与快照汇总一致；通过。代码：未改生产逻辑；无重复逻辑。命名：使用现有 sourceId。后续：p002/p003 必须按 sample/link-only 策略处理；新来源和自测仍需单独授权。
