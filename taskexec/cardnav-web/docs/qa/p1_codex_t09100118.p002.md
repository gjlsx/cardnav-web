# p002 外部分数与来源读取合同 QA

日期：2026-09-10，Asia/Hong_Kong。

## 交付与边界

- `PublicModelLeaderboardRow.score` 和 `rank` 现在可为 `null`；仅有限数值分数、正整数来源名次可被读取。缺失/非法值不再归一化为 `0` 或“第 0 名”。
- `src/leaderboard-reference-policy.ts` 只注册 p001 审计过的四个 CardNav 历史样例 URL，状态为 `sample`；未知来源为 `unverified`，公开证据对象不输出其分数/名次。
- 证据 URL 只允许无凭据的公开 HTTP(S) 地址，拒绝 `javascript:`、`data:`、`file:`、localhost、私网和回环地址；该函数只校验链接，不发出网络请求。
- 同一来源、原榜、任务和快照时间形成不可跨快照合并的键；本任务没有采集、写入 raw、seed、数据库迁移或生产发布。

## Test-first 与验证证据

1. 新增排序回归测试后，旧实现失败：`null` rank 被 JavaScript 算术当作 `0`，错误排在来源 rank `2` 前。失败输出为 `actual ['unknown-rank', 'second']`，期望 `['second', 'unknown-rank']`。
2. 实现后运行：

```text
pnpm exec tsx --test test/leaderboard-evidence.test.ts test/model-leaderboard.test.ts
7 passed, 0 failed

pnpm run typecheck
astro check + tsc --noEmit: 0 errors, 0 warnings, 0 hints
```

3. 全量 `pnpm test`：110 passed、1 failed。失败为既有 `test/public-brand.test.ts` 对 README 的“俄语支持”断言与当前 README 文案不一致；该测试/README 均不属于本任务，也未被本任务修改。其余新增 7 项榜单证据/排序测试通过。

## 自审

- 不存在综合评分、权重、100 分制、过期阈值或“方法已验证”的新声明。
- 真实零仍显示为零；未知值显示为 `-`，没有伪造来源数值。
- P3 仍负责完整 API/首屏/加载更多的来源说明展示；本任务仅完成其可安全消费的读取合同与最小空值显示。
