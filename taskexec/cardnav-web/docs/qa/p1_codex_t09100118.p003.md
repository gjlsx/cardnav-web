# p003 榜单证据展示与加载更多 QA

日期：2026-09-10，Asia/Hong_Kong。

## 交付

- SSR、JSON API 和浏览器加载更多共同消费 P2 的 `evidence` 合同。公开 API 返回 `sourceUrl`、`sourceName`、`sampledAt`、`groupKey`、`status`、`methodStatus`，并只返回 `displayScore`/`displayRank`，不会把 link-only 原始数值泄漏给浏览器。
- 每行显示参考来源、安全原榜入口、原采样时间、原榜标识、样例/引用状态和“方法/量纲未核验”限制；未提供分数/名次显示三语“未提供”，真实 `0` 仍按数值展示。
- 行以证据组键隔离并携带 `data-leaderboard-evidence-group`；多来源不会按同一数值序列混成本站名次。关联商品/中转站仍只使用既有 `leaderboardRelatedPaths`。
- 三语文字覆盖中文、英文和俄文；未新增本站评分、付费名次、外部采集或生产写入。

## Test-first 与自动验证

1. 新增 `test/leaderboard-evidence-ui.test.ts` 后，旧实现连续失败：SSR 缺 `item.evidence`，API 无 `sourceUrl`，浏览器行将 `null` 传给 `Number()`。
2. 修复后：

```text
pnpm exec tsx --test test/leaderboard-evidence.test.ts test/leaderboard-evidence-ui.test.ts test/model-leaderboard.test.ts test/p010-presentation-remediation.test.ts
12 passed, 0 failed

pnpm run typecheck
astro check + tsc --noEmit: 0 errors, 0 warnings, 0 hints
```

3. 全量 `pnpm test`：113 passed、1 failed。失败是既有 `test/public-brand.test.ts` 与未纳入本任务的 README 俄语支持文案不一致；当前任务未修改 README。此前与本任务冲突的“榜单不显示来源”断言已被收窄为：商品不显示商家提供方名，而模型榜单必须披露证据来源。

## 浏览器真实入口（Playwright）

- `pnpm exec astro dev status` 证实本次 Astro 开发服务运行；`127.0.0.1:3101` 被当前 Codex 进程占用但不可连接，Vite 自动使用实际服务 `http://127.0.0.1:3102`。未终止占用 3101 的进程。
- 在 3102 实测 `/model-leaderboard`、`/en/model-leaderboard`、`/ru/model-leaderboard`，以及三个语言的 `/model-leaderboard/coding` 任务直链：均 HTTP 200；各页 3 条样例、3 个原榜链接、1 个来源组、方法限制提示存在。
- 英文 API `/api/model-leaderboard/coding.json?offset=0&locale=en`：HTTP 200、`totalCount=3`；首行拥有所需 8 个证据字段，`status=sample`、`rank=1`、`score=1456.21`、来源为 HTTPS。
- 移动视口 375×667：无水平溢出，3 个来源链接可见。当前运行数据每任务只有 3 条样例，低于初始 30 条，故“加载更多”按设计隐藏；API/浏览器加载路径由新增回归测试覆盖，未伪造更多数据来制造按钮。

## 自审

P3 目标完成，且无必要重构。限制：当前产品仍只是历史样例展示，真实外部榜单接入、可复现自测和超过首屏的真实分页数据均不在本任务内；3101 本机端口异常需在最终环境验收时另行处理或确认。
