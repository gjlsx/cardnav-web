# p004 最小自有事件存储 QA

日期：2026-09-10，Asia/Hong_Kong。

- 默认配置为关闭；`--check-config` 仅显示非敏感限额和本机目标判断。
- 事件固定白名单，拒绝 URL、邮件、未知字段和非 UUID 身份字段；不存 IP、UA、Cookie、headers、请求正文或出站 URL。
- 新增仅两张同库表：`measurement_events`、`measurement_daily_limits`，都带 server-side `environment`；端点关闭时返回 204 且不访问 DB。
- 端点要求同源 Origin，流式累计体积，超额 413；有效/重复/限额/故障状态分别为 202/204/429/503，均 `no-store`。
- 验证：`pnpm exec tsx --test test/measurement.test.ts test/measurement-api.test.ts` 3 passed；`pnpm run typecheck` 0 diagnostics；本机 `--init-schema` 成功。临时 `MEASUREMENT_ENABLED=true` 的本机测试事件返回 `accepted`，相同 eventId 随后返回 `duplicate`。未改生产环境变量、业务表或生产环境。
- 首次建表尝试因 CLI 未加载 `.env` 使用 root 空密码而失败，未写入；已修复为加载本地 env 后成功。测试事件只在 local-test 环境，不能作为经营统计。
