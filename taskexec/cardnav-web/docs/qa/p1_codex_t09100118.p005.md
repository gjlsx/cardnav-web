# p005 匿名会话链 QA

- 服务端默认关闭时不注入脚本；允许后仅用 sessionStorage UUID，不用 cookie/localStorage/IP/UA/URL/搜索词。
- 用户可通过会话控件允许或撤回；失败 keepalive 上报不阻塞链接。榜单浏览、证据打开、比较入口与网关筛选按最小字段发送；不发送 comparison_complete。
- 验证：`test/measurement-browser.test.ts`、`test/measurement.test.ts` 通过，类型检查通过。实际生产开关未开启；本机 synthetic 事件只作 QA。
- 限制统一交给 P7：尚无真实访客/外站转化、完整多页行为基线或生产隐私审核，不能当作商业结算。
