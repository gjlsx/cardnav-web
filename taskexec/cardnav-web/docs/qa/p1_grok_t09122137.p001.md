# QA t09122137.p001 中转站排序/赞助路径审计

日期：2026-09-12 Asia/Hong_Kong  
范围：只读；无采集、无写库、无业务改动。

## 结论

服务端中转站列表已按 `score, modelCount, name` 自然排序，SQL 不含 `sponsor DESC`。赞助通过 UI sticky 重新置顶，污染主列表 SSR、筛选/动态加载和模型详情。站点详情外链缺少 `sponsored` rel。评分 tip 把展示分说成即将上线的综合评分系统。

## 证据

- `src/store.ts` `sortGatewaySites` / `mysqlGatewaySiteRows` ORDER BY 无 sponsor。
- `src/pages/llm-gateway.astro`、`src/pages/llm-gateway/models/[modelId].astro` 设置 `stickySortKey="sticky"` 且 `sticky={Boolean(site.sponsor)}`。
- `src/scripts/llm-gateway-home.js`、`src/scripts/gateway-detail-tables.js`、`src/scripts/data-table.js`、`src/scripts/deferred-table-core.js` 比较时 sticky 优先。
- `src/pages/llm-gateway/[slug].astro` 外链 `rel="noopener noreferrer"`。
- `src/i18n/zh.ts` `gatewayScoreTip` 宣称多维评分系统即将上线。
- `test/public-shop-data-boundary.test.ts` 只断言 SQL 无 sponsor DESC。

## 后续

p002 用纯函数拆分 natural/sponsored 并加三个反例测试。`/ranking-reference` 不在本单。
