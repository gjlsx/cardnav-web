# QA t09122137.p005 回归与浏览器验收

日期：2026-09-12 Asia/Hong_Kong  
入口：`pnpm exec astro dev status` → `http://127.0.0.1:3102/`（为加载本单代码重启了过期的 3102 进程；未部署、未采集）。

## Commands

| Command | Exit |
|---|---|
| `pnpm test` | 0；133 passed / 0 failed |
| `pnpm run typecheck` | 0；Astro 143 files，0 errors/warnings/hints；`tsc --noEmit` 通过 |
| `pnpm run build` | 0；server build complete |

## Browser（真实路由）

从 `/llm-gateway` 进入，再打开模型详情与站点详情；另测 `/en/llm-gateway`、`/ru/llm-gateway`。

| Case | Result |
|---|---|
| 主列表独立性说明 | 可见「完整自然榜」 |
| 无赞助 | 不渲染赞助区（section=0） |
| 自然表 sticky | 无 `data-sort-sticky="1"` |
| 筛选 | 搜索后表格仍可渲染 |
| 模型详情 | 同样独立性说明，无 sticky 置顶 |
| 站点详情 | 有说明与「报错」；本条样例外链 `rel="noopener noreferrer"`（非赞助站，正确不加 sponsored） |
| en/ru | 有 organic/органический 说明 |

截图：

- `docs/qa/p1_grok_t09122137.p005_home.png`
- `docs/qa/p1_grok_t09122137.p005_home_mobile.png`
- `docs/qa/p1_grok_t09122137.p005_model.png`
- `docs/qa/p1_grok_t09122137.p005_detail.png`

## Remaining

当前本机公开数据没有 `sponsor=true` 的中转站，因此未在浏览器里看到赞助区块实体。隔离与空态已验证；有赞助数据时应出现独立「合作商家展示」。未发布到生产。
