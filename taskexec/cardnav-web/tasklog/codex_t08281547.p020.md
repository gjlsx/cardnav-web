# codex t08281547.p020

- Detail: `details/d_t08281547.p020.md`
- Status: done
- Implementation commit: `033de65`
- Root cause: the local `ThreadingHTTPServer` test calls `shutdown()` but leaves its listening socket open because it never calls `server_close()`.
- Reproduction: strict `ResourceWarning` execution reported an unclosed `127.0.0.1` socket.

## Verification

- `python -W error::ResourceWarning -m unittest scripts.collection.tests.test_pipeline.LiveMysqlHttpTests.test_approved_local_http_ingests_to_local_mysql -v` — passed without a warning.
- `python -W error::ResourceWarning -m unittest discover -s scripts/collection/tests -v` — 35 passed.

## Self-review

- Requirement satisfied: server shutdown now closes the listener and joins its worker thread.
- Scope limited to test fixture cleanup; no collection or MySQL behavior changed.
- Result: pass:no-refactor-needed.
