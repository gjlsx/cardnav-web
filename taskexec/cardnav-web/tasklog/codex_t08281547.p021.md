# codex t08281547.p021

- Detail: `details/d_t08281547.p021.md`
- Status: done
- Implementation commit: `4131dc8`
- Root cause: the live local-MySQL acceptance test persists deliberate test observations and snapshots but has no teardown transaction.

## Verification

- The new isolation assertion failed before the fix because `collected-p013-test.example` remained in `shop_products`.
- `python -W error::ResourceWarning -m unittest scripts.collection.tests.test_pipeline.LiveMysqlHttpTests -v` — 2 passed.
- `python -W error::ResourceWarning -m unittest discover -s scripts/collection/tests -v` — 36 passed.
- Direct local MySQL check: `test_shop_rows=0`.

## Self-review

- Only test-owned `p013-http` collection rows and its unique formal shop site are removed.
- The existing shop snapshot is rebuilt in the same cleanup transaction; other sources and records are preserved.
- Result: pass:no-refactor-needed.
