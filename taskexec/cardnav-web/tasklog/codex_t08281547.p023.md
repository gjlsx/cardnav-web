# codex t08281547.p023

- Detail: `details/d_t08281547.p023.md`
- Status: done
- Implementation commit: `074a150`

## Delivered

- 四个采集子 Tab 现按选中来源提供抓取一次、开始循环抓取、停止选中来源和测试选中来源。
- `SourceRunControl` 为每来源保存 stop flag、循环标志与运行中状态；停止立即写 `task <source-id> is stop!`，后续循环停止。
- pipeline 在每条处理前和正式发布前检查 stop flag；停止 run 标记为 `stopped` 且不发布。
- 受控测试按钮只调用项目固定 `source_tests/test.py --source <id>` 并将命令、stdout/stderr 和退出状态交给本页日志。

## Verification

- `python scripts/collection/source_tests/test.py --source openprice-products` — `fixture_rows=1`, `network_requests=0`。
- `python -W error::ResourceWarning -m unittest discover -s scripts/collection/tests -q` — 39 passed。
- `pnpm test` — 93 passed。
- `pnpm run typecheck` — 0 errors。

## Self-review

- 没有硬编码来源按钮；新增来源自动适用同一操作。
- 测试入口固定在项目受控路径，未提供任意命令执行。
- draft 来源仍不发 HTTP；停止无法中断已进入 urllib 的单次读取，但读取结束即停止后续入库/发布。
- Result: pass:no-refactor-needed.
