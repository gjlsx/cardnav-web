# codex t08281547.p031

- Detail: `details/d_t08281547.p031.md`
- Status: doing

## Plan

1. Compare the recorded original requirements with the p024–p030 final baseline and current code.
2. Remove any callable legacy direct-publish route that bypasses `merge_import_batch`.
3. Update the authority note, run collector/web regressions, and record the production deployment boundary separately.

## Implementation

- Removed the unused `PublicPublisher.publish_run()` legacy direct-staging publish path and its repository-only helpers.
- Kept runtime writes intentionally limited to explicit batch merge/import plus the already-required manual website-config operation.
- Marked the original overall document with the p024–p028 current-data-flow authority note.
- Added a regression assertion that the legacy publish entry cannot return.

## Verification

- `python -W error::ResourceWarning -m unittest discover -s scripts/collection/tests -q` — 53 passed.
- `pnpm test` — 97 passed.
- `pnpm typecheck` — 0 errors, 0 warnings, 0 hints.
- `pnpm build` and `git diff --check` — passed.
- Static audit found no runtime `publish_run`, no second staging table, and no stale staging publish helper.
- Read-only SSH banner probe for `206.119.177.74:22` returned empty; no authentication or remote mutation occurred, so p008/p022 remain `partial`.
