# codex t08281547.p035

- Detail: `details/d_t08281547.p035.md`
- Status: done
- Implementation commits: `cc03d61f940c83806d2faef2f9c78faa1fb31c98`, `d619268c2324b49784366d740ce120625d446e82`
- README recovery: restored `README.ru.md` from `HEAD`; left the unrelated user edit in `README.en.md` untouched.
- Local regression: `pnpm run seed:reference-samples` succeeded (5 sources, 9 products, 7 sites); `pnpm test` passed 97/97; `pnpm typecheck` reported 0 errors/warnings/hints; `pnpm build` succeeded.
- Remote MySQL: created exactly one `ailovemoney_remote@216.144.231.55` account with only `SELECT`, `INSERT`, `UPDATE`, `DELETE` on `ailovemoney.*`; removed the pre-existing `root@'%'`; server verification found zero non-local root accounts and one approved account. Credentials were written only to ignored local `.env` and are not recorded here.
- Remote network control: enabled `ai-lovemoney-mysql-firewall.service`; its persistent nftables rule permits loopback for the application, permits only `216.144.231.55` externally on TCP/3306, and drops all other TCP/3306 traffic. The first rule version omitted loopback, which blocked the app's own `127.0.0.1` database access; corrected immediately and verified with a local TCP MySQL query.
- Publication: fresh `dist` and minimal local SQL export were uploaded; a tool execution window interrupted after `dist` extraction and before service restart, causing the existing old Node process to mismatch the new files and return 500. Root cause was confirmed from process age, direct 3101 response, and logs; then the SQL was imported, ownership restored, and only `ai-lovemoney` restarted. The temporary server upload files were removed after verification.
- Final production verification: `apache2`, `ai-lovemoney`, and `ai-lovemoney-mysql-firewall` active; Node 3101, HTTP 80, HTTPS 443, `/shops`, `/llm-gateway`, `/official-price`, `/model-leaderboard`, and `/guide` all returned 200. LikeShop `/shop` 8086, `/mobile` 8090, and `/admin` 8095 all returned 200. No collector timer or process exists on the VPS. Browser verification at `https://ai.lovemoney.live/` loaded title `AI LoveMoney - 一站式 AI 大模型账号购买导航` with the expected brand and 中转网站 content.
- Self-review: requirements satisfied; no secrets committed or logged; unrelated dirty README content preserved; no further refactor needed.
