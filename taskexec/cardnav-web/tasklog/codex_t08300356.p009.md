# t08300356.p009 执行记录

- Detail: `details/d_t08300356.p009.md`
- Claim: 2026-08-30 03:59 Asia/Hong_Kong
- Status: done

## 计划

1. 先为数据字段、排序与页面链接写失败测试。
2. 以最小 schema、snapshot、站点配置编辑和 Astro 页面改动实现需求。
3. 跑定向测试与构建；完成后交给 p010 进行真实 3101 浏览器截图验收。

## 结果

- 实现提交：`6d2c1ea`。
- 公开数据默认排序为评分降序、模型数降序、站点名稳定排序；SQL、snapshot 与参考样例都统一该规则。
- `gateway_sites` 新增 `region`、`benefit_text`；本地“网站配置”读取并真正解析可编辑的 `key=value` 字段，保存时仅更新这两个手工字段并刷新快照，采集导入不覆盖它们。
- 列表增加“最近刷新”列，删除“打开”；详情页新增福利/地区、空福利兜底、外站“领完福利前往”和 Telegram“提交建议”。
- 已通过定向 TypeScript 测试、全部 57 个 Python collection 测试、`pnpm run typecheck` 与 `pnpm run build`。全量 `pnpm test` 仅因任务开始前即已删除的 `README.ru.md` 失败，其余 93 项通过。
