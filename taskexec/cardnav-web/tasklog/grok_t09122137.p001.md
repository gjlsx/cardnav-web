# t09122137.p001 执行记录

- Detail: `details/d_t09122137.p001.md`
- Claim: 2026-09-12 21:48 Asia/Hong_Kong
- Status: doing

## Plan

1. 只读盘点中转站自然排序、sponsor/sticky、评分文案、SSR 与动态加载路径。
2. 锁定 p002–p005 最小合同与三个回归反例。
3. 不改业务代码、不采集、不写库。

## 用户确认（审查后执行口径）

1. 赞助区：列表与模型详情表格上方独立区块；数据源仍是现有 `gateway_sites.sponsor` 布尔；无赞助则不渲染。
2. `/llm-gateway/[slug]` 纳入覆盖；付费外链加 `rel="sponsored noopener noreferrer"`。
3. `/ranking-reference` 整页不在本单；只修正中转站入口上的评分/来源表述。

## 受影响入口（源码核实）

| 入口 | 路由 | 数据加载 | 自然排序 | 赞助注入 | 行组件 | 客户端 |
|---|---|---|---|---|---|---|
| 中转站主列表 SSR | `/llm-gateway` | `loadGatewaySites` | SQL / `sortGatewaySites`：score DESC, modelCount DESC, name ASC | `isStickyGatewaySite` → `sticky=true` | `GatewaySiteTableRow.astro` | `data-table.js` `compareStickyRows` |
| 筛选 / 加载更多 | 同上 + `/api/llm-gateway/sites.json` | API 再调 `loadGatewaySites`（无 limit，再 slice offset） | 服务端仍自然序 | `llm-gateway-home.js` `isStickySite(site.sponsor)`，比较时 sticky 优先 | JS 动态建行 | `llm-gateway-home.js` `compareEntries` |
| 模型详情 SSR | `/llm-gateway/models/[modelId]` | `loadGatewayModelDetail` → `mysqlGatewaySiteRows({ modelId })` 同一 SQL 序 | 同主列表 SQL | `isStickyGatewaySite` + `stickySortKey="sticky"` | 同 `GatewaySiteTableRow`（`showScore=false`） | `data-table.js` |
| 模型详情加载更多 | `/api/llm-gateway/model-sites/[modelId].json` | `loadGatewayModelDetail` 全量再 slice | 同 SQL | `gateway-detail-tables.js` `sticky: site.sponsor ? 1 : 0` | JS 动态建行 | `deferred-table-core.js` 与 detail 脚本 sticky 比较 |
| 站点详情 | `/llm-gateway/[slug]` | `loadGatewayDetail` / `loadGatewaySiteBySlug` | 单站，无榜 | 标题旁 `SponsorBadge`；外链 `benefitForward` 现 `rel="noopener noreferrer"` | 详情页本身 | `sponsor-badge.js` |

模型 Tab（`/llm-gateway` 的模型列表）按支持站点数排序，不用 sponsor sticky，本单不改其排序。

## 排序差异

- **服务端自然规则已存在且不含 sponsor**：`src/store.ts` `sortGatewaySites` 与 `mysqlGatewaySiteRows` 的 `ORDER BY gateway_sites.score DESC, COALESCE(coverage_summary.model_count, 0) DESC, gateway_sites.name ASC`。
- **现有测试只锁 SQL**（`test/public-shop-data-boundary.test.ts`），不锁浏览器 sticky，所以当前测试无法抓住赞助置顶。
- **主列表、筛选后、模型详情共用同一自然比较键**，差异只在：模型详情表不显示 score 列，但仍用 sponsor sticky 打乱行序。
- Snapshot 路径：`loadGatewaySites` 读 `gateway-sites` 快照后走 `sortGatewaySites`；MySQL 路径直接用 SQL。两者自然键一致。

## 评分与来源文案（不可称为 AIGATE 综合评分）

| 字段 | 位置 | 用户可见 | 本单处理 |
|---|---|---|---|
| `site.score` / `siteScore` | 主列表列、站点详情数字卡 | 列名「评分」；tip「评分系统根据模型数量、价格、用户评价、稳定性等多维因素决定，仅供参考，即将上线。」（en/ru 同类） | **必须改语义**：这是既有展示分，默认/常见 50，不是已上线的本站综合评分 |
| `sourceName` | 行内「来源: …」、详情 meta | 来源名 | 保留；不得标 sponsored |
| Hvoy `sourceRank` / `userRating` | `/ranking-reference?tab=transit` | 已有「上游排名算法未公开，不代表 AIGATE 自有综合评分」 | **本单不改该页** |
| 样例 `score=50` | `reference-samples` / seed | 展示分 50 | 不重新解释为新公式 |

`formatPositiveScore`：非正数显示 `-`，正数两位小数。隔离后自然榜仍用该函数展示同一 `siteScore`，不得因 sponsor 改值。

## 回归反例（p002 失败测试必须锁）

1. **赞助状态变更**：同一自然输入只把某站 `sponsor` false→true。`natural` 的 id 顺序、`siteScore` 完全不变；仅 `sponsored` 集合增加该站。
2. **无赞助输入**：全部 `sponsor=false`。`sponsored` 为空；`natural` 等于现有 `sortGatewaySites` 结果。
3. **双路径**：自然本应上榜的合作商家。`sponsored` 含该站，`natural` 也含该站且名次仍按 score/modelCount/name，不得因购买从自然榜删除或前移。

## 最小改动文件集（p002–p004）

- 新增：`src/gateway-ranking.ts`（纯函数拆分 natural / sponsored；不依赖 DOM）
- 新增：`test/gateway-ranking.test.ts`
- 改：`src/store.ts` 仅在公开加载处调用拆分或保持 `sortGatewaySites` 给 natural（不改 SQL、不改 schema）
- 改：`src/pages/api/llm-gateway/sites.json.ts`、`src/pages/api/llm-gateway/model-sites/[modelId].json.ts` 返回隔离后的 items + sponsored
- 改：`src/pages/llm-gateway.astro`、`src/pages/llm-gateway/models/[modelId].astro`、`src/pages/llm-gateway/[slug].astro`
- 改：`src/components/GatewaySiteTableRow.astro`（去掉自然表 sticky 用法；行内徽章可保留为合作标记，但自然序不再用它）
- 改：`src/scripts/llm-gateway-home.js`、`src/scripts/gateway-detail-tables.js`；视需要 `data-table.js` / `deferred-table-core.js` **只取消网关表 sticky 键，不改商店**
- 改：`src/i18n/zh.ts`、`en.ts`、`ru.ts`（评分 tip、赞助区标题、隔离说明；纠错复用现有「报错」→ Telegram）
- 可能：`src/components/SponsorBadge.astro` 仅当需要更明确的文字标签

## 不该触碰

- `src/shop-sponsored-pinning.ts`、`test/shop-sponsored-pinning.test.ts`、商店 `ORDER BY shop_sites.sponsor DESC`
- `scripts/crawlee_collection/**`、`scripts/collection/**`、`catch.config`、GUI
- 采集/merge/import、新 schema、新评分公式、部署、生产统计
- `/ranking-reference` 整页、`/shops`、首页赞助卡 `src/sponsors.ts`（除非共享组件最小安全修复并记录）
- 未跟踪本地 GUI / `database.py` / `.gitignore` 等脏改

## p002 合同

- 新纯函数 `splitGatewaySiteRanking(sites)`：`natural = sortGatewaySites(sites)`（sponsor 不是比较键）；`sponsored = natural.filter(s => s.sponsor === true)`。
- 无赞助时 `sponsored.length === 0`，调用方不渲染区块。
- JSON 与 SSR 使用同一函数；客户端不得再按 sponsor sticky 重排自然表。
- 自然表分页仍切 `natural`；赞助区块展示当前筛选集合内全部赞助站（不过滤出自然榜）。

## p003 / p004 合同

- 主列表与模型详情：赞助区块在表上方，有文字标题（不得把「赞助」译成「推荐」或「自然排名」）；自然表去掉 `stickySortKey` 与 `data-sort-sticky` 赞助用法。
- 站点详情：赞助徽章保留；`site.outboundUrl` 的「领完福利前往」在 `site.sponsor` 时使用 `rel="sponsored noopener noreferrer"`，非赞助保持 `noopener noreferrer`。Telegram/QQ 社群链不是付费目标。
- 披露紧邻列表/赞助块：自然榜与赞助独立；`siteScore` 不是 AIGATE 综合评分。纠错复用现有 `t.llmGateway.reportIssue` → Telegram，不承诺工单/SLA。
- 中英俄 locale 语义一致。

## 本任务验证

- 只读源码与现有测试文件；未运行采集、未写 MySQL、未改业务文件。
- 未启动 `pnpm test` 作为本审计的完成条件（无行为变更）。
