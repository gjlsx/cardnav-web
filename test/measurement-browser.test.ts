import assert from 'node:assert/strict'; import fs from 'node:fs'; import test from 'node:test';
test('browser measurement requires session consent and keeps outbound navigation non-blocking', () => { const source = fs.readFileSync('src/scripts/measurement.js', 'utf8'); assert.match(source, /sessionStorage/); assert.match(source, /keepalive: true/); assert.match(source, /\.catch\(\(\) => \{\}\)/); assert.match(source, /utm_source/); assert.match(source, /WeakSet/); assert.match(source, /data-measurement-entry/); assert.doesNotMatch(source, /localStorage|document\.cookie|preventDefault/); });
